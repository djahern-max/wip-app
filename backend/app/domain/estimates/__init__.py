"""Estimates (F06, D-32): the platform's estimate template loaded into the spine.
Pure modules (``names``, ``versions``, ``totals``, ``exceptions``) take rows and return
values; ``normalize`` writes the tables from raw records; ``service`` reads for the API."""
