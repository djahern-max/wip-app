# Copy to .env for local development. Values match db/init/01_roles.sh defaults.
# DATABASE_URL, CRYPTO_KEYS and CRYPTO_ACTIVE_KEY_ID have no defaults: the API refuses
# to start without them. DATABASE_OWNER_URL is needed only where Alembic runs.
# Application role (no BYPASSRLS, not table owner)
DATABASE_URL=postgresql+psycopg://app_rw:app_rw_dev@localhost:5433/wip
# Migration role (owns the schema). Used only by Alembic.
DATABASE_OWNER_URL=postgresql+psycopg://app_owner:app_owner_dev@localhost:5433/wip
# Test suite (pytest) uses a separate database
TEST_DATABASE_URL=postgresql+psycopg://app_rw:app_rw_dev@localhost:5433/wip_test
TEST_DATABASE_OWNER_URL=postgresql+psycopg://app_owner:app_owner_dev@localhost:5433/wip_test

# F02 · application-layer encryption (app/core/crypto.py). No key material lives in this
# file or anywhere in the repo. Generate one key per environment with:  python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
# Format: key_id:base64key[,key_id:base64key…]. Rotate by adding a key and moving the active id.
CRYPTO_KEYS=dev1:<base64 of 32 random bytes; see command above>
CRYPTO_ACTIVE_KEY_ID=dev1
# Sessions: idle and absolute limits, cookie flags. SESSION_COOKIE_SECURE=false only for a
# browser that refuses Secure cookies on http://localhost (Chrome and Firefox accept them).
SESSION_IDLE_MINUTES=60
SESSION_ABSOLUTE_HOURS=12
SESSION_TOUCH_SECONDS=60
SESSION_COOKIE_SECURE=true
# Base of the React app: prefix of one-time activation links, and the only origin
# allowed to make state-changing browser requests (F02.1).
APP_BASE_URL=http://localhost:5173
# F02.1 · activation links (D-16) and the enrolment-only session they open.
ACTIVATION_LINK_TTL_HOURS=72
ENROL_SESSION_TTL_MINUTES=15
# F02.1 · per-IP throttle on login, TOTP verify/recover and link redemption.
IP_THROTTLE_FAILURES=20
IP_THROTTLE_MINUTES=15
# Reverse proxies in front of the API: 0 trusts the socket address only; N takes the
# N-th address from the right of X-Forwarded-For (docs/OPERATIONS.md).
TRUSTED_PROXY_COUNT=0
# Path of this file at runtime (default: repo-root .env). Point it elsewhere in a
# deployment, or at a missing path to disable file loading.
# ENV_FILE=/etc/wip/.env
# Host port for the compose Postgres (docker-compose.yml).
POSTGRES_HOST_PORT=5433

# F03 · object storage (D-21). "local" keeps objects under LOCAL_OBJECT_STORE_DIR
# (default: repo-root .object-store, gitignored). "s3" is DigitalOcean Spaces: the
# five SPACES_* variables have no defaults and are required only then; the API and
# worker exit at start-up naming any that is missing.
OBJECT_STORE=local
# LOCAL_OBJECT_STORE_DIR=/var/lib/wip/objects
# SPACES_ENDPOINT_URL=https://nyc3.digitaloceanspaces.com
# SPACES_REGION=nyc3
# SPACES_BUCKET=
# SPACES_ACCESS_KEY_ID=
# SPACES_SECRET_ACCESS_KEY=
# Lifetime of a signed download URL (S3 only) and the upload size limit.
SIGNED_URL_TTL_SECONDS=60
MAX_UPLOAD_BYTES=26214400
# F03 · worker (D-19). Poll interval (LISTEN/NOTIFY wakes it earlier), task lease,
# attempts and backoff (base × 2^(attempt−1), capped). See docs/OPERATIONS.md.
WORKER_POLL_SECONDS=5
WORKER_LEASE_SECONDS=300
TASK_MAX_ATTEMPTS=5
TASK_BACKOFF_BASE_SECONDS=30
TASK_BACKOFF_CAP_SECONDS=900
