# CHANGELOG

Newest first. One entry per closed feature or decision. Format:

```
## YYYY-MM-DD · Fxx · Title
**What**: …
**Why**: …
**Migrations**: …
**Tests**: …
**Decisions referenced / made**: D-xx
**Dependencies added**: …
**Follow-ups**: …
```

---

## 2026-09-17 · F02.1 · owner pass and enrolment auto-select
**What**: The owner's browser pass is done (Chrome, 2026-09-17): activation link with the token cleared from the address bar, password, QR identical after a refresh, code accepted, recovery codes, sign-in with password and code, company switch, sign-out, spent link refused. Ticked on the owner's instruction, each noted "owner pass, 2026-09-17": the last criterion in `docs/briefs/F02.1.md` and the frontend criterion in `docs/briefs/F02.md`. Gap found in the pass and fixed: the single-company rule now lives in one function, `auto_selected_tenant()` in `app/auth/service.py`, called by password login and by `confirm_totp_enrolment`; at confirm it sets the tenant on the enrolment-only session before the rotation, so `_complete_login` writes `tenant_enter` (`via: auto`) to that tenant's `audit_log` in the same transaction and `login_success` carries the tenant.
**Why**: A user with exactly one company was auto-selected at sign-in but landed on "Choose…" after enrolling through a link, because the link's session starts with no tenant and the rotation copied that.
**Tests**: 231 pass (229 + 2): a firm user with one entry row has `active_tenant_id` set in the confirm response and in `/me`, with exactly one `tenant_enter` row (`via: auto`, `firm_staff`) and the tenant in `login_success`; a user with two entry rows lands with none selected and no `tenant_enter` row. The first was red before the change (`active_tenant_id` was `None`); the second passed before and after, and pins the rule's other half.
**Migrations**: none. **Decisions referenced / made**: none. **Dependencies added**: none.
**Follow-ups**: one new Discovered note in `current-feature.md` (a client user's optional enrol confirm records a second `login_success`).

---

## 2026-09-17 · F02.1 · enrolment follow-ups
**What**: `backend/tests/test_frontend_effects.py`: a static check that fails when a `useEffect` / `useLayoutEffect` body in `frontend/src` calls `api(` with a method other than GET, directly or through a function defined in the same file, unless the file is in `GUARDED` (only `pages/TotpEnrol.jsx`, with the reason). Comments are ignored; `api(` with a non-literal method and `fetch(` with a `method` option inside an effect are refused rather than interpreted; a stale `GUARDED` entry fails; the entry's guard (request held in a ref, StrictMode still on) is asserted. It does not follow helpers imported from another module. `Activate.jsx` disables its submit button while the request is in flight, like Login, TotpVerify and the TotpEnrol confirm form, which already did.
**Why**: The enrolment bug came from a POST in a mount effect, and nothing automated looked at effects. A double click on the activation form posted the single-use token twice and showed "invalid link" over a success.
**Tests**: 229 pass (222 + 7): four on the scanner itself (the original enrolment effect is flagged; GETs, a commented-out call and requests in handlers pass; same-file functions are followed; unreadable calls are refused), three on the tree. **Mutation checks done by hand, each reverted**: (a) the `TotpEnrol.jsx` entry removed from `GUARDED` → `test_no_effect_sends_a_non_get_request_outside_the_guarded_list` fails naming `pages/TotpEnrol.jsx` line 21 `api("POST")`; (b) an effect calling `switchTenant()` added to `Shell.jsx` → the same test fails naming `switchTenant() → api("POST")`. `npm run build` passes. The owner browser-pass criterion stays unticked.
**Migrations**: none. **Decisions referenced / made**: none. **Dependencies added**: none.

---

## 2026-09-17 · F02.1 · enrolment idempotency fix
**What**: `POST /api/auth/totp/enrol` is idempotent while an enrolment is pending: it locks the user row (`SELECT … FOR UPDATE`, re-read under the lock) and returns the stored unconfirmed secret, generating one only when none is pending; already enrolled is still 409. A pending secret (secret present, `totp_enrolled_at` NULL; no schema change) is dropped by `_clear_pending_totp()` when a new activation link is issued, when a link is redeemed, and at a successful password login (a client user starts optional enrolment from a normal session, so the next login is the "later enrolment session"); admin TOTP reset already cleared it; confirm turns it into the enrolled secret. `TotpEnrol.jsx` keeps its one enrol request in a ref, so StrictMode's doubled mount effect sends one request; StrictMode stays on.
**Why**: Owner browser pass. React 18 StrictMode ran the mount effect twice in dev, two concurrent enrol calls each wrote a new secret, the server kept the last write and the screen showed either one, so codes never matched. A refresh, a double click or a retry did the same in production. The F02 end-to-end check was the API flow through the Vite proxy with one call per step, and every backend test called enrol exactly once, so no check ever made the second call.
**Tests**: 222 pass (217 + 5): two sequential enrol calls return the same secret and URI, the secret is stored as key-ring ciphertext, a code from the first response confirms, and the same ciphertext becomes the enrolled secret; two concurrent calls return the same secret (the test holds the user row lock until both requests queue on it; with `FOR UPDATE` removed the test fails with two different secrets, checked by hand and reverted); issue-link and admin TOTP reset clear a pending secret at once and the next enrolment gets a different one; redemption clears on its own; a client user's abandoned secret is gone at the next login. Response scan and log capture pass unchanged. `npm run build` passes. The owner browser-pass criterion stays unticked.
**Migrations**: none. **Decisions referenced / made**: D-16. **Dependencies added**: none.
**Follow-ups**: see "Discovered" in `current-feature.md` (pending secret is per user, not per session; `Activate.jsx` has no double-submit guard; proposed static check for non-idempotent requests in mount effects).

---

## 2026-09-17 · F02.1 · CI first-run fix
**What**: `alembic/env.py` reads `MigrationSettings` (`DATABASE_OWNER_URL` and nothing else) instead of the full `Settings`; no migration uses the keys. The test harness is hermetic: `tests/__init__.py` imports `tests/_env.py` before anything imports the application (and `_env.py` refuses to load otherwise); it generates a throwaway key ring with `os.urandom` per run, points `ENV_FILE` at a missing path, and `conftest` drops every other `Settings` variable from the environment. The fixed test keys are gone. `ci.yml`: checkout, setup-python and setup-node moved to v7; no crypto key is set because no step needs one.
**Why**: The first real CI run failed at the migration step with `missing required environment variable(s): CRYPTO_ACTIVE_KEY_ID, CRYPTO_KEYS`, because `env.py` built the full `Settings`. The Tests step would have failed next: an import sort had moved `tests._env` below the application imports in `conftest`, and `app.main` builds `Settings` at import, so the suite was reading the developer's `.env` and passed locally only because that file existed.
**Tests**: 217 pass (214 + 3): alembic up/down as a subprocess with only `DATABASE_OWNER_URL`; its absence names that variable alone; the suite's env file does not exist and the wrong import order is refused. 217 pass with `.env` renamed (before the change: exit with the CI message). CI reproduced locally in a clean shell (empty Postgres, fresh clone and venv, only `ci.yml`'s variables), every backend step green. No key-shaped string in any tracked file.

## 2026-09-17 · F02.1 · Auth hardening patch
**What**: Firm authority is an explicit row (D-15): new global table `firm_membership(firm_id, user_id, role ∈ {firm_admin, firm_staff})`, unique per firm and user, CHECK-constrained to firm roles. `membership.role` becomes nullable with a CHECK allowing only NULL or a client role: a NULL row is an *entry row* that lets a firm user open that tenant with the authority of their `firm_membership`. Exactly one function, `app.core.auth.effective_role`, computes the role that applies inside a tenant; a static test proves nothing else reads `membership.role`. An entry row whose user has no `firm_membership` in the tenant's firm is an orphan and grants no access (403 on switch, absent from the tenant list). `firm_admin` routes for firm memberships (`GET/POST /api/admin/firm-memberships`, `PUT/DELETE …/{user_id}`) audited to `firm_audit_log`; the last `firm_admin` cannot be removed or demoted, with the firm row locked `FOR UPDATE` before counting (two concurrent demotions leave one admin and one 409). Removing a firm membership commits first (row, audit, sessions), then removes entry rows one tenant per transaction (owner answer A; a simulated failure part-way leaves no access and a retry finishes). `POST /api/admin/tenants` creates a bare tenant in the caller's firm (owner answer C). A user holds either a firm role or client roles in one firm, never both; the API refuses the mix. Accounts activate only through an admin-issued link (D-16): users are created with no password; `POST /api/auth/activate` sets it and, for a firm user without TOTP, opens a 15-minute enrolment-only session (`ENROL_SESSION_TTL_MINUTES`) in which the existing enrol/confirm routes finish the job and show the recovery codes; a firm user without TOTP cannot log in by password (generic failure); issuing a link voids the earlier one and ends sessions; admin TOTP reset and password reset both return a new link; `/api/auth/password/reset` and the `password` field on user creation are gone; the `password_reset_*` columns are renamed `activation_*`. Every session of a user ends on password change (the caller's too), link redemption, TOTP reset, and any `firm_membership` change; removing a membership clears `active_tenant_id` and keeps the session. Per-IP throttle (`app/core/throttle.py`) on login, TOTP verify and recover, and activation, counted from `firm_audit_log`: 20 failures in 15 minutes → one `ip_throttled` row, 429 for the rest of the window, nothing else written; the check runs before any argon2 verification; new index on `firm_audit_log(ip, occurred_at)`. Client IP from `X-Forwarded-For` only with `TRUSTED_PROXY_COUNT` > 0 (N-th from the right; socket address when the header is missing or short). Unknown-e-mail failures store `email_sha256`; the `email` key is refused in audit detail. Null-firm audit rows are shown to `firm_admin` only (D-17). Every user-bearing response goes through an allow-list schema (`app/api/schemas.py`); credential columns on `user` are a deferred group loaded only by the auth service; a suite-wide scan checks every JSON body for the credential column names and stored values. `Settings`: `DATABASE_URL`, `CRYPTO_KEYS`, `CRYPTO_ACTIVE_KEY_ID` have no defaults and the process exits naming the missing variable; `DATABASE_OWNER_URL` is optional and required only by Alembic; `ENV_FILE` selects the env file. `.env.example` holds no key material; CI fails if `.env` is tracked; ruff pinned to 0.16.8 in the dev dependencies and CI. The middleware refuses preflights and state-changing requests whose `Origin` differs from `APP_BASE_URL` (no `Access-Control-*` headers ever). The probe router moved to `tests/probes.py`; the application package no longer mentions pytest. The production engine is built with `hide_parameters=True` and `echo=False` (the harness overrides it to keep parameter capture), and a `DBAPIError` handler logs SQLSTATE plus the primary message only. D-18: `read_as_user()` in `app/core/auth.py` is the single way to read another user's `membership` rows (swap `app.user_id`, restore in `finally`, callable only from `app/auth/admin.py`, results filtered to the actor's firm). `scripts/create_user.py` gained subcommands `bootstrap` (firm + first `firm_admin`, no tenant needed), `create-user`, `issue-link`, `reset-totp`; it prints the link once to stdout and never sets a password. Frontend: `Activate.jsx` replaces the reset-password page and continues into TOTP enrolment. Docs: CLAUDE.md amendments (tenant-scope naming rule, approved global tables, data migrations never unforce RLS, the D-18 `read_as_user()` sentence, one live brief, commit-at-close-out rule), closed briefs moved to `docs/briefs/`, D-15…D-18 appended to `docs/DECISIONS.md`, OPERATIONS runbooks for bootstrap, links, throttle and `TRUSTED_PROXY_COUNT`.
**Why**: The F02 review found that a wrong role on one client granted firm-wide administration, that a stolen password alone could enrol the second factor, that credential changes left sessions alive, that anonymous callers could grow an insert-only table without bound, and that nothing but discipline kept credential columns out of responses. D-15, D-16 and D-17 settle the model while the auth tables hold no real users. The owner's conditions on option (b) (one role function, orphan rows = no access), on removal (no task iterates tenants inside one transaction), on the data step (never unforce RLS), and on the swap helper (D-18) are recorded in the F02.1 brief. The idle-timeout bookkeeping in `get_principal` moved to its own short transaction because holding a row lock on the session for the whole request deadlocked with an admin ending that user's sessions concurrently.
**Migrations**: `0003_firm_membership_activation` (reversible; verified up → down → up on a scratch database, alone and with 0002, and on the local `wip`). Creates `firm_membership`, renames the two activation columns, adds the IP index, and runs the data step per tenant with `app.tenant_id` set: one `firm_membership` per (firm, user) with the highest F02 role, firm-role membership rows become entry rows, abort with the user ids if anyone holds a firm role and a client role in one firm. Downgrade restores roles from `firm_membership`; orphan entry rows (which grant nothing) are the only lossy part and are reported with a NOTICE.
**Tests**: 214 passing (F02: 150). New: `test_firm_membership.py` (roles from `firm_membership`, CHECK constraints, entry-row flow with `membership_created` then `tenant_enter`, mutual exclusion, last-admin guard and atomic audit, concurrent demotions with the firm row locked, orphan rule, two-phase removal with simulated failure and retry, bootstrap on an empty database through to tenant creation and entry), `test_activation.py` (no password at creation, single use, expiry, firm flow with 15-minute session and gated routes, TTL, supersession, TOTP reset, no password-only enrolment, session invalidation matrix, CLI link printed once and never logged), `test_throttle.py` (21st failure 429, one row, 100 further attempts add nothing, no argon2 when throttled, link failures count, forged header ignored, proxy count 1 and 2, missing or short header), `test_hygiene.py` (e-mail hashed, null-firm rows for firm_admin only, start-up refusal naming the variable, `.env` untracked and example key-free, origin policy, no pytest in the app, probes 404 normally, production engine options, IntegrityError without bound values, D-18 helper restore and write behaviour, single call site, single role-read site), `test_zz_response_scan.py` (names, stored values, tokens only in `activation_url`, never in audit detail). Changed: role matrix (+7 routes, 3 scratch states), auth, admin, audit, RLS (entry row assertion), migrations (0003 data step and refusal), log leaks (attempted e-mail against application records, activation tokens verbatim). **Mutation checks done by hand, each reverted**: (a) a second read of `Membership.role` outside `effective_role` (in `change_membership_role`) → `test_membership_role_is_read_only_inside_effective_role` fails; (b) a second call site for `read_as_user()` (in `redeem_activation_link`) → `test_read_as_user_is_called_only_from_the_admin_service` fails; (c) `password_hash` added to the `UserOut` response schema → `test_no_credential_column_name_in_any_json_body` fails. Post-close additions (owner, 2026-09-17): the activation token travels in the URL fragment (`/activate#token=…`), which browsers never send to a server; the page reads it, clears the address bar with `history.replaceState`, and posts it in the body; a test asserts no backend route declares a query parameter named like a token and that a token in the query string is ignored. `get_principal` reads the session row, the user, memberships and firm memberships inside the request transaction; only the `last_seen_at` write is a separate short transaction, and it happens only when the value is at least `SESSION_TOUCH_SECONDS` (60) stale; tests: a session ended by an admin is 401 on its very next request, a fresh session is not rewritten and a stale one is, and the idle and absolute expiry tests still pass. 214 tests.
**Decisions referenced / made**: D-15, D-16, D-17 (owner, staged file appended), D-18 (owner constraints, written here). Corrects the F02 follow-up: D-15 does **not** make `firm_audit_log.firm_id` NOT NULL; unknown-e-mail failures, failed link redemptions and IP throttles have no firm.
**Dependencies added**: none.
**Follow-ups**: owner browser pass through the CLI (closes the F02 and F02.1 frontend criteria); admin UI for users, memberships and links (Discovered, not built); re-encrypt command for key rotation arrives with F03; Postgres echoes the conflicting key in an `IntegrityError` DETAIL line, so what the application logs about a database error is limited to SQLSTATE and the primary message (`describe_db_error`).

---

## 2026-09-17 · F02 · Auth, roles, audit log
**What**: Hand-rolled authentication (D-10): e-mail + argon2id password (12+ chars), 5-failures-in-15-minutes lockout with identical "unknown e-mail" / "wrong password" responses, TOTP (RFC 6238, ±1 step, stored time-step counter so a code cannot be replayed) mandatory for `firm_admin` / `firm_staff` and optional for client roles, ten hashed one-time recovery codes, `firm_admin` password-reset links (returned to the admin, no e-mail) and TOTP resets, and `scripts/create_user.py` for bootstrap and last-resort resets. Server-side sessions in the new global `session` table (256-bit id in an `HttpOnly; Secure; SameSite=Lax` cookie, only the SHA-256 stored; id rotates at login and at TOTP verification; idle 60 min and absolute 12 h from environment; logout deletes the row). CSRF middleware requires `X-Requested-With` on every `POST/PUT/PATCH/DELETE` under `/api/`. Tenant context now comes only from the session: `require_tenant_id` reads the principal, the `X-Tenant-Id` header is gone; `get_principal` sets `app.user_id` and `app.tenant_id` with `set_config(..., true)` on the one request transaction. D-11 own-membership `FOR SELECT` policy via `allow_own_membership_read()`, and `tests/test_rls.py` now allow-lists extra policies (exactly one). Tenant switcher (`GET /api/session/tenants`, `POST /api/session/tenant`), auto-selection for single-membership users. `app/core/authz.py`: `require_roles` / `require_firm_role` and named capabilities (`can_view_pay_rates` = firm_admin, firm_staff, client_admin, …); a test-only probe router mounts one route per capability under pytest. `firm_admin` endpoints for users, memberships (in any tenant of the firm, including one with no members yet), resets. Audit (D-12, D-13): tenant-scoped `audit_log` (RLS) and global `firm_audit_log`, both insert-only via `make_append_only()` triggers and registered in `APPEND_ONLY_TABLES`; plain JSON read routes `GET /api/audit` (firm roles + client_admin) and `GET /api/firm-audit` (firm roles). `app/core/crypto.py`: AES-256-GCM key ring with key id stored beside the ciphertext, used for TOTP secrets. Frontend: login, TOTP verify (with recovery-code option), TOTP enrolment (QR + manual key, recovery codes shown once), reset-password page, shell with tenant switcher and sign-out; unauthenticated users see only the login page. Housekeeping from the F01 review: compose host port from `POSTGRES_HOST_PORT` (default 5433); this CHANGELOG template gained **Tests** and **Dependencies added**. Also fixed: `Settings` now reads the repo-root `.env` when run from `backend/` (F01's defaults had hidden that it was never loaded).
**Why**: BLUEPRINT §3.8 and §11: every request is tied to an authenticated user, a role, and a tenant taken from the server-side session, never from the client. Firm-level authority is **derived from memberships** (no schema change; see the F02 plan for the `firm_membership` proposal, D-15 candidate). Expected failures that must be persisted (failed login, failed code, lockout) are returned as results and rendered as 401/429 so the request transaction commits; failures that must roll back (403 on tenant switch) raise. `session` stores the active tenant as `active_tenant_id` so the RLS enumeration does not mistake the global table for a tenant table. `firm_audit_log.firm_id` is nullable because an unknown-e-mail failure has no firm.
**Migrations**: `0002_auth_audit` (reversible; verified up → down → up on a scratch database in a test and on `wip` via the CLI). Adds credential columns to `user`; creates `session`, `audit_log` (RLS + append-only), `firm_audit_log` (append-only); creates the `own_membership_read` policy and the `raise_append_only()` trigger function.
**Tests**: 150 passing (F01: 25). New: role matrix (5 roles × 22 protected routes, plus 401 unauthenticated on each), pay-rate probe, TOTP gate for unenrolled and unverified firm sessions, replay, recovery code single use, lockout on the sixth attempt with identical bodies, `X-Tenant-Id` ignored, own-membership read via ORM and raw SQL (zero rows without `app.user_id`; insert rejected, update/delete match nothing with it), tenant switch without membership 403 and unchanged, extra-policy allow-list, `audit_log` isolation, append-only for `app_rw` and `app_owner` including drop/disable attempts, audit rows and atomicity of role change + audit row, firm audit readable by firm roles only, cookie flags and hash-only storage, rotation, idle and absolute expiry, logout, CSRF, key rotation, crypto ring, and a suite-wide log capture (SQLAlchemy statement logging on) that finds no password, secret, code, recovery code, or session id. **Mutation checks done by hand**: a `sneaky` policy on `_rls_probe` and a second policy on `membership` each make the enumeration test fail; a registered append-only table without triggers fails (in-process test). End-to-end against the local stack through the Vite proxy: login → enrol (QR/secret) → confirm → tenant list → admin joins a tenant without prior membership → switch → audit reads → logout → replayed code refused; cookie flags as specified. The React screens were not driven in a browser in this session (no browser automation available); `npm run build` passes.
**Decisions referenced / made**: D-10, D-11, D-12, D-13, D-14 (appended to `docs/DECISIONS.md`). Proposal for D-15 (`firm_membership`) recorded in `current-feature_02.md`.
**Dependencies added**: backend `argon2-cffi`, `pyotp`, `cryptography`; frontend `qrcode` (all pre-approved by D-10/D-14).
**Follow-ups**: owner decision on D-15 (firm authority table; makes `firm_audit_log.firm_id` NOT NULL); re-encrypt command for key rotation arrives with F03 token storage; a firm user without TOTP who holds a stolen password can enrol their own TOTP (inherent to the brief's enrolment rule; mitigated by admins creating users with reset links and watching `totp_enrolled`); browser pass of the React flow by the owner. See "Discovered" in `current-feature_02.md`.

---

## 2026-09-17 · F01 · Project skeleton & tenant isolation
**What**: Repo layout per BLUEPRINT §12 (`backend/`, `frontend/`, `docs/`, `db/`, `tests/fixtures`, `tests/golden`); `docs/BLUEPRINT.md` moved from the repo root. Docker compose Postgres 16 on port 5433 with a bootstrap script that creates `app_owner` (schema owner, migrations) and `app_rw` (application; not owner, `NOSUPERUSER`, `NOBYPASSRLS`) and grants `app_rw` DML through default privileges so migrations never name the role. FastAPI app with `/api/health`, config from environment, and a request-scoped session dependency that opens a transaction and sets `app.tenant_id` with `set_config(..., true)` (`SET LOCAL`). `tenant_session(engine, tenant_id)` context manager for workers and scripts. Alembic baseline with `firm`, `tenant`, `user`, `membership`, and the test-only `_rls_probe`. Migration helper `enable_tenant_rls(table)` that enables **and forces** RLS and creates the `tenant_isolation` policy. React + Vite shell that calls the health endpoint. GitHub Actions CI: ruff, migration up/down/up, pytest, frontend build. README and OPERATIONS runbooks.
**Why**: BLUEPRINT §3.8 and §11: isolation is enforced by the database, not by remembering a `WHERE` clause. Doing it in the first migration means every later table inherits the guarantee and the CI test (`tests/test_rls.py`) enumerates every `tenant_id` table so a missed helper call fails the build. Policy uses `NULLIF(current_setting('app.tenant_id', true), '')::uuid` so a missing or expired setting yields zero rows and rejected writes rather than a cast error.
**Migrations**: `0001_baseline` (reversible; downgrade drops the five tables and the `membership_role` enum). Verified up → down → up on an empty database in a test and via the CLI.
**Tests**: 25 passing. RLS enabled+forced+policy+leading index on every `tenant_id` table; cross-tenant reads return nothing via ORM and raw SQL as `app_rw`; cross-tenant inserts and updates are rejected; no context → zero rows, insert fails, also for the owner (FORCE); `app_rw` cannot disable/unforce RLS or drop the policy; context does not leak across pooled transactions; request dependency scopes correctly per header; product name appears in exactly one constant per side. Mutation check done by hand: an unprotected `tenant_id` table makes the enumeration test fail.
**Decisions referenced / made**: none. D-09 (name) still open; working name lives in `backend/app/core/product.py` and `frontend/src/product.js` only.
**Dependencies added**: fastapi, uvicorn, sqlalchemy, alembic, psycopg[binary], pydantic-settings; dev: pytest, httpx, ruff. Frontend: react, react-dom, vite, @vitejs/plugin-react.
**Follow-ups**: F02 replaces the `X-Tenant-Id` header placeholder in `require_tenant_id` with the authenticated tenant. See "Discovered" in `current-feature.md` for the rest.

---

## 2026-09-17 · Project inception
**What**: BLUEPRINT, ROADMAP, CLAUDE.md, and F01 brief created from review of Rye Beach Landscaping's chart of accounts, balance sheet, P&L (YTD 9/17/2026), and two LMN estimator closing reports.
**Why**: Establish design before code. Key constraints found: LMN has no general API (file import); QBO Projects GraphQL API is partner-gated (use core REST API); Ramp passes QBO projects through its Customer/Job field (no Ramp integration needed in v1); payroll posts to the GL in lump sums (labor job cost must be computed from timesheets).
**Decisions referenced / made**: D-01…D-09 open; see BLUEPRINT §14.
**Follow-ups**: Phase 0 checklist (BLUEPRINT §13).
