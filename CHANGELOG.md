# CHANGELOG

Newest first. One entry per closed feature or decision. Format:

```
## YYYY-MM-DD · Fxx · Title
**What**: …
**Why**: …
**Migrations**: …
**Tests**: …
**Decisions referenced / made**: D-xx
**Dependencies added**: …
**Follow-ups**: …
```

---

## 2026-09-25 · F05.1 · QBO production connection
**What**: Rye Beach's own QuickBooks Online company is connected read-only to tenant `rye-beach` on jobcost.dev with Intuit production keys (scope `com.intuit.quickbooks.accounting` only, asserted on the stored scope); Intuit's production questionnaire was answered from OPERATIONS.md and the keys installed by the owner; 13 months × 4 document types (Aug 2025–Aug 2026) tie to QuickBooks' own reports to the cent (owner, 2026-09-23; the three first-pass differences were QuickBooks export errors, none a platform cause). **Webhooks**: `POST /api/qbo/webhook`, unauthenticated and outside the session and CSRF machinery, reads the raw body and checks `intuit-signature` (base64 HMAC-SHA256 with `QBO_WEBHOOK_VERIFIER`, `compare_digest`) **before anything is stored: an unsigned or badly signed delivery is rejected with 401 before any insert**, counted per address by the in-process throttle, and a good signature is never throttled; with no verifier the route answers 503. A good delivery is stored raw, one `webhook_event` row per CloudEvents event (legacy shape accepted; anything else stored as one row with no realm), in one untenanted transaction, answered 200, and only then dispatched in a background task: one `cdc:{connection}:now` poll per connected realm (the same dedupe key as Sync now, so a burst is one poll), `connection.last_webhook_at` set, unknown or not-connected realms counted and left alone. `webhook_event` is tenant-less and append-only (D-29); **a static test (`tests/test_webhook_event_reads.py`) pins it to its two tenant-less reads**, the store in the webhook route and the 24-hour count the Connections page shows, and fails on any third reference in the application package. **Intuit-side disconnect**: the portal disconnect URL is `/api/qbo/disconnected?realmId=`; the route enqueues one poll for a connected realm (never marks anything on the query string) and 303s to the static page; the poll's refused refresh sets `needs_reconnect`, audited. A connection made under the other Intuit environment ends as `needs_reconnect` with `environment_mismatch` at its next use, once, never retried. **`scripts/delete_tenant.py`** (D-28): operator-only, owner role, refuses `PROTECTED_TENANT_SLUGS` and a non-owner role, prints counts, requires the slug typed back, revokes tokens at Intuit, deletes the Space objects, then one transaction with the append-only triggers disabled for that transaction only and the rows deleted in catalog-derived foreign-key order, ending with a firm-level `tenant_deleted` row; first real use removed `qbo-sandbox` from production, `prod_check.py` passing afterwards. **Questionnaire error handling**: `QboError`/`NeedsReconnect` carry Intuit's `intuit_tid`, stored on the failed `sync_run.detail` and the `connection_needs_reconnect` audit row; support line in the signed-in footer. **Small fixes** carried from F05.0: Imports download sends `Content-Disposition` with the original filename (the Spaces path through `ResponseContentDisposition` on the signed URL); the TOTP enrolment page survives a reload; OPERATIONS.md "Environments". Connections page shows "Last webhook received" and the 24-hour count. Disconnect verified live on `rye-beach` (Disconnect, Reconnect, one poll; `revoked_at_source` on the audit row; totals unchanged) and the production webhook path verified by the owner pass (a memo edit visible within five minutes with no Sync now), both 2026-09-25, in place of the sandbox-on-jobcost.dev steps, which the early key swap made impossible.
**Why**: D-25: the production connection follows the first deployment; the WIP schedule needs Rye Beach's real billing, and webhooks make a change in QuickBooks reach the copy in minutes with polling kept as the safety net. D-28 and D-29 record the two exceptions this feature needed (deleting a tenant; a table without `tenant_id`).
**Migrations**: `0009_webhook_event` (`webhook_event`, tenant-less, `make_append_only`, partial unique index on the event id; `connection.last_webhook_at`). Reversible; round trip in `test_migrations`. Production `wip` is at 0009.
**Tests**: 592 backend (563 → 592), 12 frontend; lint clean; CI never reaches Intuit or the droplet. Signature against an `openssl dgst` vector with wrong, missing and body-modified cases each 401 and storing nothing; five deliveries in a second → five rows, one poll; a retried delivery stores once; unknown realm and needs-reconnect enqueue nothing; per-IP throttle on bad signatures only; 503 without a verifier; the disconnect page's poll sets `needs_reconnect`; the environment-mismatch guard; `delete_tenant.py` end to end on a scratch tenant, the protected slug refused, `app_rw` refused; the static `webhook_event` read check (mutation-checked: a third reference fails it); download filename on both stores; TOTP reload; env-template parity. Live checks by the owner as above.
**Decisions referenced / made**: D-28, D-29 (made with this feature), D-25, D-27. Owner answers are in `docs/briefs/F05.1.md` (Plan).
**Dependencies added**: none.
**Follow-ups**: three criteria stay open for the owner in `docs/briefs/F05.1.md`: the account-number check (Chart of Accounts export), S-01 question 4 (three Ramp-synced Purchase/Bill ids; BLUEPRINT §13.7), the phone pass tick. The connection day, realm id, backfill duration and the `qbo-sandbox` deletion counts are on the audit rows, not in the repo. Discovered (carried in the live stub): the API service's `app.*` INFO logging; a webhook during a running poll waits for the next scheduled poll; CDC after a customer `Merge` (F07); the tie-out findings for F08 / D-02; the `test_rls` slot collision. Next: F06 · Estimate import.

---

## 2026-09-24 · Docs · D-30 pooled supplies
**What**: Docs: D-30 pooled supplies applied to BLUEPRINT §5/§7/§8.1/§8.3/§8.5/§13.2, ROADMAP (F13.1), CLAUDE.md vocabulary; D-31 (`revenue_method` is the field) applied to the §5 sentence and the vocabulary entry. No code.
**Why**: East Coast Landscape Supply invoice #3919 is job cost that belongs to no single job; D-30 settles how such purchases are booked (a pool job) and how they reach jobs (month-end allocation, formatted reclass entry posted by the controller).
**Migrations**: none.
**Tests**: none; documentation only.
**Decisions referenced / made**: D-30, D-31 (made); D-04, D-23, D-24 unchanged.
**Dependencies added**: none.
**Follow-ups**: F13.1 · Pool allocation (ROADMAP, after F13). F07 gains the `revenue_method` value `pool`; F06 expects no estimate for a pool; F10 pool lines are `gl_direct` with job = pool job.

---

## 2026-09-22 · F05.0 · First deployment (jobcost.dev)
**What**: The platform runs at https://jobcost.dev on one DigitalOcean droplet (nginx, `wip-api` and `wip-worker` as systemd services under user `wip`), Managed Postgres 16 and the private Space `jobcost-files` (D-27). Under `deploy/`: `setup.sh` (idempotent bootstrap: user, venv, NodeSource Node 24 pinned by `frontend/.nvmrc`, nginx, certbot webroot, swap, env files installed once and never overwritten), `deploy.sh` (under a lock: fetch and detached checkout as `wip`, pip, `npm ci` and a Vite build into `dist.new`, `alembic upgrade head` as root from `/etc/wip/migrate.env` **before** any restart, frontend swap, restart, health check through nginx that tells "no answer" from a 503 whose body names the database; the served commit is recorded in `/opt/wip/.deployed` and reported from there), `env.template` and `migrate.env.template` (the API and worker never hold the owner URL), the nginx site with `www` and `http` redirects, HSTS, the security headers and a Content-Security-Policy the bundle satisfies (`img-src data:` for the TOTP QR code only; CI fails on an inline script or style in the built `index.html`), the two units with `ProtectSystem=strict`. `backend/scripts/prod_check.py` reports role flags, RLS enabled and forced on every tenant table, the D-11 allow-list, the append-only triggers, SSL and a HEAD on the bucket from the shared catalog queries in `app/tenancy/catalog.py` (the same ones the RLS and append-only tests use). Static pages `/privacy`, `/terms`, `/qbo/disconnected` (plain HTML, `pages.css`, no script) name Ryze Group, Inc. and `admin@jobcost.dev`. The worker's `LISTEN` connection carries TCP keepalives so a silently dead peer is replaced within about a minute. `01_roles.sh` takes `PG_MAINTENANCE_DB` for a managed cluster. `S3ObjectStore.open` spools boto3's non-seekable body to a temporary file so a parser that sniffs and rewinds behaves as it does against the local store (found by the D-21 check: the chart upload failed with `UnsupportedOperation`). OPERATIONS.md "Production (jobcost.dev)": layout and who may read what, first-time set-up in order, deploying, rolling back, proving the failed-migration path on a scratch database, restarting, certificates, backups, `prod_check.py`, firewall. Live on 2026-09-22: first deploy, `prod_check.py` all 9 checks (twice), the failed-migration proof on `wip_scratch` (stopped at `migrate`, services and `dist` untouched, scratch rolled back), the D-21 upload and download against the Space, and the owner's pass from a phone on LTE (TOTP sign-in, Connections showing the same month totals as on the Mac, the change poll running unattended, the pages without sign-in).
**Why**: BLUEPRINT §12 and §3 principle 10: the smallest production shape that Intuit's production review can point at (a public host, a privacy policy and terms at stable URLs naming a legal entity) and that the owner already knows how to operate (superCPE runs the same way). F05.1 needs it before production keys can be requested.
**Migrations**: none. The production database `wip` is at 0008, created by `01_roles.sh` and `deploy.sh`; `PROTECTED_DATABASE_NAMES=wip` in both env files.
**Tests**: 563 backend (533 → 563), 12 frontend; lint clean. `tests/test_deploy.py`: the env template and `.env.example` declare the same names (mutation-checked by hand both ways), the pages carry every required statement and no script, the CSP header is pinned, both scripts parse with `bash -n`, `deploy.sh` locks then builds then migrates then swaps then restarts and records the served commit after the restart, `setup.sh` never overwrites an env file, `prod_check.py` passes on the test database and fails when a check is broken. `tests/test_storage.py`: the S3 store yields a seekable stream from a body that refuses `seek`. No test needs the droplet; nginx site files were checked locally in an nginx 1.24 container.
**Decisions referenced / made**: D-27 (made with this feature), D-17, D-21 (upload/download against Spaces done 2026-09-22), D-22, D-25. Owner answers are in `docs/briefs/F05.0.md` (Plan).
**Dependencies added**: none in the application. Server packages: nginx, certbot with the nginx plugin, Node 24 from NodeSource.
**Follow-ups**: F05.1 (production keys, the questionnaire, launch and disconnect handling, Rye Beach read-only, webhooks, the Ramp line check). Discovered and carried in the live stub: `Content-Disposition` on Imports downloads, the TOTP enrolment page after a reload, a delete-tenant command, an "environments" note in OPERATIONS.md. F23: monitoring, restore drill, key rotation, CI deploys.

---

## 2026-09-21 · F05 · QBO connection & sync (sandbox)
**What**: A firm admin connects a tenant to a QuickBooks Online company (OAuth2; the callback takes tenant and user from the single-use `state`, never from the session; one company per tenant, enforced by a partial unique index; a different company is refused once the tenant holds any QuickBooks record). Tokens are ciphertext bound to tenant, connection and field; refresh runs under a row lock in its own transaction, replaces both tokens from every response, and a refused refresh sets `needs_reconnect` once, audited, and stops that connection's tasks without retries. The platform keeps a read-only copy: every §6.2 entity stored raw (insert-only, versioned; a CDC delete is a stub that becomes a deleted version carrying the last known payload), then normalized into `customer`, `billing`, `billing_line`, `payment`, `payment_application` by pure, idempotent, order-independent normalizers (cents exactly or the payload is skipped and counted; SubTotal line first; discount as its own total and line; `subtotal − discount_total + tax_total = total` as a CHECK; the three-part void rule; a changed payment replaces its applications wholesale; a sales receipt is one billing row plus one fully applied payment of its own kind). Backfill by Id, one task per page; Change Data Capture polling every 15 minutes with the successor enqueued first and a 29-day cursor refusal; a nightly count-and-totals check that ends `drift` and re-pulls nothing; accounts attached to the chart by account number only, from the Account normalizer and from the chart follow-on. Connections page: status in words, Connect / Sync now as the one primary action, attention items (needs reconnect, fresh backfill needed, drift, skipped payloads, accounts without a number, duplicate numbers, numbered-not-in-chart, chart-not-in-QuickBooks, attached n of m, unlinked deposit lines), records held, month totals in `.table-wrap` with a totals row summed by exact string arithmetic. Spike S-01 run against the sandbox (`docs/spikes/S-01.md`). Built and accepted against the Intuit sandbox only (D-25).
**Why**: BLUEPRINT §6.2: QuickBooks is the book of record and the platform needs an exact, traceable copy of the sales side before F07/F08 can tie billing to jobs. D-25 moved the production connection to F05.1.
**Migrations**: `0007_qbo_connection` (connection company, token bookkeeping, pending OAuth state; unique `(system, realm_id)`; `sync_run.detail`), `0008_billing_tables` (the five tables, RLS enabled and forced, total-identity CHECK). Both reversible; round trips in `test_migrations`; the dev `wip` database was upgraded only.
**Tests**: 533 backend (438 → 533), 12 frontend (11 → 12); lint clean; CI never reaches Intuit (the harness refuses every call; `tests/qbo_helpers.FakeIntuit` and `FixtureCompany` stand in). Fixtures recorded from the sandbox (`tests/fixtures/qbo_sandbox/`, README there) with a month-totals oracle that shares no code with the normalizers. Mutation checks by hand, reverted: the access-log filter (test fails without it), the newest-version check in normalize. Owner pass on the live sandbox, 2026-09-21: August totals equal QuickBooks' Transaction List by Date for all four columns; a deleted invoice left the totals after Sync now; a payment then a void on two invoices behaved as specified; disconnect/reconnect resumed the sync; chart attachment 24 of 25 with 6150 and 1400 named.
**Decisions referenced / made**: D-25 (recorded this feature), D-19, D-20, D-22. Owner answers of 2026-09-20 and 2026-09-21 are in `docs/briefs/F05.md` (Plan → Owner answers; S-01 answers).
**Dependencies added**: `httpx` moved from dev to runtime (pre-approved in the brief; D-14). No Intuit SDK, no OAuth library, no scheduler.
**Follow-ups**: F05.0 (first deployment) then F05.1 (production keys, Rye Beach, webhooks, the Ramp line check). The nightly drift check and the 29-day refusal are proven in tests, not yet seen live. Discovered items are in the live stub. Restart `make worker` after pulling this.

---

## 2026-09-20 · F04 · owner pass
**What**: The owner's browser pass is done (Chrome, 2026-09-19, steps 1–10 with the fixture csv: 159 loaded, 0 skipped; 159 suggestions, 29 in job cost; Cost codes grid correct; one suggestion edited by hand; confirm all → 0 of 159 unmapped; `wip_basis` set with reference D-04; burden rate added, an overlapping one refused; Accounts at phone width). The fixes that came out of the pass were checked live by the owner on 2026-09-20: the table pattern on Accounts at phone width, the Source selection kept on Imports, Imports cards below 640 px. On the owner's instruction the criterion is ticked in `docs/briefs/F04.md` with the note "owner pass 2026-09-19; fixes checked 2026-09-20", F04 is ☑ in ROADMAP, and `current-feature.md` is the closed stub pointing at F05 (S-01 is its first task).
**Why**: The last F04 acceptance criterion, which only the owner ticks.
**Tests**: no code change; 438 backend + 11 frontend tests as of c8a81c0.
**Migrations**: none. **Decisions referenced / made**: none. **Dependencies added**: none.

---

## 2026-09-20 · F04 · nothing loaded status; only the newest chart is applied
**What**: Owner answers on 2792364 (already pushed, so this is a new entry). (1) New import status `nothing_loaded`, shown as "Nothing loaded": rows were found and none could be read. An end state like the others and not `failed`: the task succeeds, nothing follows, the sentence is unchanged ("No accounts could be read from this file. …"), and the Imports page shows it in the error style. A batch with no rows at all (e.g. `unparsed_file`) is still `loaded`. (2) `normalize_chart` applies a batch only if it is the tenant's newest chart batch by `uploaded_at` (among batches that hold a chart or are being read: `loaded`, `loaded_with_issues`, `processing`; a `failed` or `nothing_loaded` batch supersedes nothing). An older batch processed late changes no account, mapping or suggestion, writes no audit row, and records `import_batch.followup_outcome = superseded`; the API reports follow-up status `superseded`, label "Not applied", message "Loaded. Accounts not updated, because a newer chart of accounts was uploaded after this file." An applied batch records `applied`. (3) Docs: `docs/briefs/F04.md` records the local parse result of the owner's workbook (160 accounts, 9 rows skipped) as the evidence that the layout is supported, the owner's decision that the workbook is never re-processed on the dev database (the fixture's names are the deliberate anonymization; the dev rye-beach tenant stays on the fixture), that no requeue path for succeeded tasks is needed, and leaves account 2630 under Discovered for the owner. OPERATIONS: the status list, what to do after "Nothing loaded", what "Not applied" means.
**Why**: "Loaded with issues" is wrong when nothing was loaded. The ordering rule was found while checking what a re-run of the older workbook batch would do on the dev database: it would have put an older file's names over a newer chart's (1 account added, 33 confirmed accounts renamed). The re-run was not done and will not be.
**Migrations**: `0006_import_batch_nothing_loaded` (reversible; round trip verified on a scratch database in a test; applied to the local `wip` with `make migrate`, upgrade only). `ck_import_batch_status` gains `nothing_loaded`; new nullable column `import_batch.followup_outcome` with `ck_import_batch_followup_outcome` (NULL, `applied`, `superseded`). The upgrade rewrites no row: the owner's 2026-09-19 06:13 workbook batch stays `loaded_with_issues`, 0 loaded, 170 skipped (checked after the upgrade) and reads with the same sentence. The downgrade turns `nothing_loaded` rows into `loaded_with_issues` one tenant at a time with `app.tenant_id` set, RLS never disabled or unforced. `0004` now carries its status list as a literal instead of importing the model's constant, so it keeps creating the constraint it always created; its effect is unchanged. **The column is an addition beyond the CHECK change the owner approved**: it is how the follow-up status can say "not applied" without misusing `task.last_error`.
**Tests**: 438 backend (435 + 3), 11 frontend unchanged, `npm run build` passes. An older chart (other names, one extra account, one missing) dated before the loaded chart and processed after it: account names, active flags, mappings, statuses and rules identical before and after, no audit row, the batch itself `loaded` 159, follow-up `superseded` / "Not applied" with the sentence, the newer batch still "Accounts updated". The zero-row test now expects `nothing_loaded` / "Nothing loaded", a succeeded task at attempt 1 and no follow-on; a batch shaped like the owner's pre-0006 row still gets the sentence. Migration: 0006 up/down/up in the round trip, and a data test with two tenants (down: `nothing_loaded` → `loaded_with_issues` in both, RLS still enabled and forced; up again: no row rewritten). **Mutation check done by hand, reverted**: the newest-batch check disabled → the ordering test fails on the account state.
**Decisions referenced / made**: D-22 (status is words first). **Dependencies added**: none.
**Follow-ups**: the owner's browser-pass criterion is still unticked and ROADMAP still shows F04 ◐, both the owner's to change. Account 2630: owner supplies an updated fixture and oracle if it should be added. Restart `make worker` after pulling this (it does not reload code).

---

## 2026-09-19 · F04 · owner workbook layout; zero-row guard; source selection kept; table pattern
**What**: (1) `parse_chart` finds the account-number column instead of assuming the first one: the starting column is the first filled cell of the header row (now also "No.", "Acct", "#"; a header needs at least two filled cells, so a one-cell title that contains "account" is no longer taken for it) or, without a header, of the first row that starts with an account number; it is fixed once per file, and number, name and type are read from there. First sheet only, as before. (2) A batch with zero loaded rows never enqueues its follow-on (`process_batch_now`), and `normalize_chart` never deactivates from an empty `present` set (second guard, for a follow-on run by hand). (3) `batch_message`: zero loaded rows reads "No accounts could be read from this file. Check that it is a chart of accounts export and upload it again."; the nouns come from two new `SourceKind` fields (`records_noun`, `file_noun`), so no domain code names a source. (4) Imports keeps the chosen Source per company for the browser session (`frontend/src/sourceChoice.js`, guarded `sessionStorage`, key carries the tenant id): the page is unmounted when another screen is opened and on refresh, which is where the choice was lost. (5) Table pattern in `styles.css`: `.table-wrap` scrolls both ways inside itself with `max-height: 75vh` / `75dvh` and a plain border; header row held at the top, first column held at the left, top-left cell above both (z-index 3 > 2 > 1), held cells opaque; first column `min-width` 12rem / `max-width` 22rem (9.5rem fixed below 640 px) with a plain right border; words never break in headers or the first column (`.wrap-anywhere` remains the opt-in, used for file names; removed from the Accounts column); `border-collapse: separate` so borders travel with held cells; `width: max-content; min-width: 100%` with a 20rem cell maximum, because `width: 100%` squeezed every other column to one word per line before the box scrolled. Docs: the owner's pass recorded in `docs/briefs/F04.md` (criterion not ticked, ROADMAP unchanged); OPERATIONS "Loading a chart of accounts" describes the supported layout and the zero-row case.
**Why**: Owner browser pass, 2026-09-19. The owner's real workbook ended "Loaded with issues", 0 loaded, 170 skipped, and the screen said "the rest were loaded. Accounts updated." Cause, in structural terms: the chart is on the first of three sheets and its used range is B1:D172: column A is empty on every row; the account number is in column B as an integer cell (General format, 160 of them), the name in C and the type in D as text; no merged cells, no cell combining number and name (five names merely begin with digits); title in B1, header "No. / …" in row 4, nine heading rows with only column B filled; panes frozen. The parser read the account number from the first column, so all 170 non-blank rows were `empty_account_number`. The follow-on then ran against a file holding no account: on a tenant with a confirmed chart and a newest batch, that path would have marked every account inactive.
**Tests**: 435 backend (428 + 7), 11 frontend (7 + 4), `npm run build` passes. A synthetic workbook built in the test with openpyxl reproduces the layout (three sheets, empty column A, title, header in row 4, integer account cells, a name beginning with digits, frozen panes, a number on another sheet): 5 loaded, 4 skipped, accounts and suggestions right; before the fix it gave 0 loaded, 10 skipped. Parser unit test: with and without a header, shifted columns, a title that contains "account", "Notes" not taken for "No.". A revised chart with zero readable rows on a tenant with confirmed mappings: every account active, every mapping untouched, no `config.normalize_chart` task, no follow-up on the batch, one audit row (the upload), the new sentence; `normalize_chart(present=set())` deactivates nothing. The oracle test, the five-digit tenant test and the earlier owner-layout test pass unchanged. Static: the table pattern's rules (bounded box, held header and first column, z-order, right border, no word-breaking outside the opt-in), no `wrap-anywhere` in Accounts, Imports never returns to a hard-coded source; `node --test` for the source choice (per company, remount, unknown source, storage that throws). **Mutation check done by hand, reverted**: the `counts.loaded > 0` guard removed → the zero-row test fails with `('succeeded', 'Updated')`. **Rendering check**: a static harness with the real stylesheet and 159 synthetic rows in headless Chrome at 375 px (iframe) and 1280 px, top and scrolled: header and account column held and opaque, words intact, sideways scrollbar on screen, the page does not scroll sideways. The real file was parsed locally from the object store: 160 accounts, 9 rows skipped; nothing of its content is in the repo.
**Not done, owner's answer needed**: re-processing the stored workbook on the dev database. It would not be "none changed": 160 loaded, 9 skipped, **1 account added (2630) and 33 confirmed accounts renamed** (the workbook's names differ from what the fixture csv loaded; ledger types all equal), and because that batch is older than the csv batch it would put an older file's names over a newer chart's. See `docs/briefs/F04.md` Discovered.
**Migrations**: none. **Decisions referenced / made**: D-22. **Dependencies added**: none.
**Follow-ups**: status for a batch from which nothing could be read: proposal `nothing_loaded`, shown as "Nothing loaded" (needs the `ck_import_batch_status` CHECK changed, so a migration); until the owner answers it stays "Loaded with issues" with the new sentence.

---

## 2026-09-19 · F04 · worker source-kind registration; batch never stranded at processing
**What**: `app/integrations/base.py` holds `SOURCE_KIND_MODULES` and `load_source_kinds()`, the one place that loads the production source kinds. `app.main` calls it (its side-effect import of `app.integrations.chart_of_accounts` is gone) and the worker calls it through the new `load_worker_modules()` in `app/worker/runner.py` (task kinds, then source kinds), which `run_forever` runs at start-up. In `process_batch_now` the source-kind lookup moved inside the handled region, so every exception after the `processing` commit passes through the `finally` that writes the status back; a source kind the worker does not know is an environment fault and follows the existing transient rule (batch `received`, error `LookupError (will retry)`, task retried with backoff). OPERATIONS: "An import batch that stays at Processing" added to the stuck-task section. `docs/briefs/F04.md` Discovered: why the suite missed it, and the real workbook's layout (below).
**Why**: Owner browser pass, step 2. A chart uploaded through the UI went Received → Processing and stayed there; the worker logged `outcome=failed error=LookupError` on every attempt. `python -m app.worker` never imports `app.main`, and only that import chain registered `chart_of_accounts`, so the worker knew `unparsed_file` alone; the lookup sat between the `processing` commit and the `try`, so the batch was never written back. The suite runs the API and the worker in one process, so the kind was always registered there.
**Tests**: 428 backend (425 + 3), 7 frontend unchanged. A fresh process that runs `load_worker_modules()` without importing `app.main` sees exactly `['chart_of_accounts', 'unparsed_file']`; every module under `app/integrations` that calls `register_source_kind(` is in `SOURCE_KIND_MODULES` and nothing else is; a batch whose kind is unregistered in the worker ends `received` with `LookupError (will retry)`, the task `queued` at attempt 1, and loads once the kind is registered. Before the fix the third test failed with `('processing', None)`. **Mutation check done by hand, reverted**: `load_source_kinds()` removed from `load_worker_modules()` → the fresh-process test fails with `['unparsed_file']`.
**Recovery on the dev database**: old worker stopped, task `d0b79561…` requeued with the OPERATIONS runbook, worker restarted. `import.process_batch` and `config.normalize_chart` both succeeded at attempt 1. The batch ended `loaded_with_issues`, **0 rows loaded, 170 skipped**, no accounts and no suggestions: the real workbook keeps column A empty and the parser reads the account number from the first column. Not fixed here; see `docs/briefs/F04.md` Discovered. The browser-pass criterion stays unticked.
**Migrations**: none. **Decisions referenced / made**: none. **Dependencies added**: none.

---

## 2026-09-18 · F04 · seeding at tenant creation; D-24 recorded
**What**: Cost categories are seeded when a tenant is created: `admin.create_tenant_and_seed` creates the tenant row in its own transaction and, once it has committed, seeds the D-23 list in a second transaction under the new tenant's context (`seed_cost_categories_at_creation`, attributed to the creator, `via: tenant_created`); `POST /api/admin/tenants` and `scripts/create_user.py create-tenant` both go through it. The lazy path (`ensure_cost_categories`) stays only as a safety net for tenants that predate F04: it is attributed to `system` (never the user whose read triggered it, `via: lazy`) and takes the tenant's transaction-scoped advisory lock before re-checking, so two concurrent first reads produce exactly one set. D-24 (time-and-materials work is its own job, off the WIP schedule) appended to `docs/DECISIONS.md` verbatim; the owner's request named it D-04, a slip, since D-04 is still open in BLUEPRINT §14.
**Why**: Owner review of F04: the list must exist immediately after creation, and a GET must never write anything attributed to the caller.
**Tests**: a tenant created through the API has the fourteen categories immediately with one audit row by the creator, and the CLI the same with actor `None`; a later read seeds nothing more; two threads seeding a pre-F04 tenant concurrently yield `[0, 14]`, fourteen rows and one audit row with actor `None`; a signed-in user's GET writes nothing attributed to that user.
**Migrations**: none. **Decisions referenced / made**: D-23, D-24. **Dependencies added**: none.

---

## 2026-09-18 · F04 · Tenant configuration
**What**: Seven tenant tables in migration 0005 (`division`, `cost_category`, `gl_account`, `account_map`, `account_suggest_rule`, `tenant_policy`, `burden_rate`; RLS on each; no seed data in the migration) plus `import_batch.followup_task_id`. Domain code under `app/domain/config/` (CLAUDE.md: domain logic lives in `app/domain/`): the D-23 category list seeded per tenant by the audited service (`ensure_cost_categories`), the cost-code function (computed, never stored), the suggestion engine (`rules.py`: regex over the whole account-number string of any length, division explicit or from a 1-based digit position, category explicit or from the last two characters, first match wins, a failed derivation = no match, no match = unmapped; patterns compiled and validated at load, refused naming the rule), the chart normalizer (`chart.py`: latest raw record per account → `gl_account`; renamed with the old name in the audit detail; missing from the newest file → inactive; the account set comes from the stored file itself because an unchanged account writes no raw row), the suggestion step (never touches a confirmed row, removes a stale suggestion), the chained task `config.normalize_chart` enqueued in the batch's final transaction via `SourceKind.after_load` with `dedupe_key = config.normalize_chart:<batch id>`, accounting-policy keys with no default anywhere (`require_policy` raises naming the key; money as Decimal, string in and out), burden rates (half-open periods, no overlap per division, `burden_rate_on(date)`, `ROUND_HALF_UP`), audited services for every create/change/confirm/deactivate with before and after ("Confirm all" = one row per account). Source kind `chart_of_accounts` (`.csv`/`.xlsx`; a row is rejected when its number cell is empty or not an account number; XLSX cells read as text). Routes under `/api/config/*` with `can_view_tenant_config` (firm roles + client_admin), `can_manage_tenant_config` (firm roles), `can_set_policy` (firm_admin). Batch API carries `followup_status`/`followup_label` and the message "Loaded. Updating accounts…" / "Loaded. Accounts updated." / "Loaded, but the accounts could not be updated. Try uploading again, or contact support." (machine detail in `error_detail` and the task's `last_error`). Frontend: Configuration with Accounts (unmapped count on top, filters, edit/confirm, "Confirm all suggestions" with a confirmation step, `.table-wrap` with the account column held), Divisions, Cost categories, Cost codes grid ("no account"), Burden rates (`formatRate`, string in), Policy ("Not decided", who/when/reference, `formatMoney`). `scripts/load_suggest_rules.py`. CLAUDE.md Vocabulary carries the D-23 list and the "Cost code" line; D-23 appended to DECISIONS.md. Fixture `account_suggest_rules.json` reshaped to the row form (results unchanged; rule 13 = amendment A). Dependency: `openpyxl`.
**Why**: BLUEPRINT §7 (configuration tables), §8.3 (WIP basis via `account_map.in_job_cost` and policy), §11; D-22, D-23; the owner's answers to the three plan calls and amendments A (catch-all outside 4000–5999), B (no four-digit assumption), C (follow-on visibility).
**Tests**: 423 backend (357 + 66) and 7 frontend. `account_map_expected.csv` is the oracle: the Rye Beach chart (159 accounts) imported through the API and the worker equals it row for row, all `suggested`, 29 in job cost, none confirmed until a person confirms; the oracle was also re-derived from the rules by a throwaway script before coding (zero mismatches) and is never edited or generated. Also: same chart twice = one batch, no change; revised chart renames/adds/deactivates with confirmed mappings and raw history intact; the owner's workbook layout rejects six rows individually and loads the rest; suggestions idempotent and never overwriting a confirmed row; amendment A (5195 → no row, unmapped, count 160); a tenant without rules → zero suggestions; amendment B (five-digit tenant with its own rules); rule validation naming the rule; amendment C (queued/succeeded/failed follow-on wording, machine detail only in `error_detail`); the follow-on enqueued once per open batch; the load script; policy (not decided, `require_policy`, firm_admin only, who/when/reference, before/after audit, money string↔Decimal with `jsonb_typeof = number`, the no-default static test); burden (overlaps, boundaries, Decimal type, strings in and out); cost categories (exact D-23 list per tenant, audited once, per-tenant statement), cost code 410 / none without a digit, grid "no account" cells; confirm-all writes N rows; RLS zero rows of another tenant for all seven tables; role matrix for every `/api/config` route; migration 0005 alone reversible with no seeded rows; frontend static checks (no inline style, effect scanner, `parseFloat`), `npm test`, `npm run build`. A real bug the tests caught before close-out: the normalizer deactivated every unchanged account on a revised chart (unchanged accounts write no raw row), fixed by taking the account set from the stored file.
**Migrations**: 0005 (seven tables + one column; reversible). **Decisions referenced / made**: D-22, D-23. **Dependencies added**: `openpyxl` (approved).
**Follow-ups**: owner browser pass (unticked); Discovered in `docs/briefs/F04.md` (no rules screen; lazy category seeding; `ledger_type` vs QBO types; a general money-as-number response assertion).

---

## 2026-09-18 · F03 · owner pass
**What**: The owner's browser pass is done (Chrome, 2026-09-18, with `make api`, `make web`, `make worker` running): a file uploaded as "Unparsed file" reached Loaded after a manual Refresh; the same file uploaded again showed the duplicate notice with still one row; the download opened and matched; with the worker stopped a different file waited at Received and reached Loaded once the worker was restarted; at phone width the list became cards and the page did not scroll sideways. Ticked on the owner's instruction, noted "owner pass, 2026-09-18", in `docs/briefs/F03.md`; F03 flipped to ☑ (2026-09-18) in `ROADMAP.md`; `current-feature.md` rewritten as the closed stub pointing at F04. `docs/DECISIONS.md` holds D-19 through D-22 exactly once each; the staged `DECISIONS_D-19_to_D-21.md` is gone from the repo root.
**Why**: The last F03 acceptance criterion, which only the owner ticks.
**Tests**: no code change; 357 backend + 5 frontend tests as of 2f6d5ad.
**Migrations**: none. **Decisions referenced / made**: none. **Dependencies added**: none.

---

## 2026-09-18 · F03.1 · interface baseline
**What**: D-22 applied to every screen. One hand-written stylesheet, `frontend/src/styles.css`, imported once in `main.jsx`: system font stack; one accent colour as `--accent`, used only by `a`, `.button-primary` and `.link-button`; no gradients, shadows or animations; every inline `style` object removed from `Login`, `Activate`, `TotpVerify`, `TotpEnrol`, `Shell`, `Imports` and `App` (the shared `styles` export is gone). Table pattern in the stylesheet: `.table-wrap` scrolls sideways inside its own container, the first column is `position: sticky`, `.num` right-aligns with `font-variant-numeric: tabular-nums`, `tr.totals` is distinct; Imports uses `.num` for Size, Rows loaded and Rows skipped and keeps its cards below 640 px (a comment says reports F08+ must use the container-scroll pattern). Primary actions: "Upload file", "Sign in", "Save password", "Verify code", "Confirm code", "I have saved these codes", one per screen, in the accent colour. Human labels: `SourceKind.label` (registration refuses a kind without one; `unparsed_file` → "Unparsed file"), `IMPORT_STATUS_LABELS` in `app/ingest/models.py` (the one status → label mapping), `batch_message()` (one sentence: what happened and what to do next). The API returns `source_label`, `status_label`, `message` beside `source_kind`, `status`, `error_detail` (the machine detail, renamed from `error`); `GET /api/imports/source-kinds` returns `label`. Upload refusals are plain sentences ("This file is larger than 25 MB. Upload a smaller file." / "Test CSV files must end in .csv. …" / "Choose a source from the list." / "The file could not be stored. Try again in a minute."); the machine reason is logged with the request id, never shown. Frontend error strings rewritten the same way; the page never interpolates `source_kind`, `status` or `error_detail`.
**Why**: Owner review against the D-22 section (2026-09-18): inline styles, unaligned numbers, "Upload", machine tokens and exception types on screen.
**Tests**: 357 backend (351 + 6) and 5 frontend. New `tests/test_frontend_styles.py`: no `style={{` in `frontend/src` (allow-list empty; mutation-checked by hand: an inline style appended to `Shell.jsx` fails naming the line, reverted), exactly one stylesheet imported once, system font and no decoration, the accent used only by the three selectors and no colour literal outside `:root`, one primary action per screen, the Imports page renders display fields only. `test_imports.py` asserts the labels, messages and `error_detail` for received / failed / loaded-with-issues / transient batches and the four refusal sentences, and that no message names a setting or exception type. `test_the_test_csv_kind_never_exists_in_a_normally_started_app`: a fresh process importing `app.main` has exactly `['unparsed_file']` (the test kind is registered by `tests/conftest.py` only; `app/` never imports `tests`, also asserted). Effect scanner, `parseFloat` check, response scan, `npm test` and `npm run build` pass.
**Migrations**: none. **Decisions referenced / made**: D-22. **Dependencies added**: none. **Behaviour change**: labels and messages only; the `error` response field is now `error_detail`.
**Follow-ups**: owner browser pass (unticked). The "Uploaded by" cell shows "unknown user" for a CLI upload (no such path exists today).

---

## 2026-09-18 · F03 · close-out fixes
**What**: (1) Destructive-downgrade guard in `alembic/env.py`: `alembic downgrade` (and `make downgrade`) runs only when `ALLOW_DESTRUCTIVE_DOWNGRADE=1` **and** the target database name is not in `PROTECTED_DATABASE_NAMES` (default `wip`); otherwise it exits non-zero naming the database, before connecting. The suite sets the flag (it downgrades `wip_test` and `wip_mig_*` only); CI runs the round trip on `wip_scratch` and proves the guard refuses `wip`. CLAUDE.md Workflow 7 added verbatim. (2) Parse errors fail fast: `PermanentTaskError` in the queue; a source's `parse` raising marks the batch `failed` and the task `failed` with no retry (`parse failed: <Type>`, attempts 1); failing to open the object or a lost database connection leaves the batch `received` with `… (will retry)` and the task retries with backoff; a `failed` batch never goes back to `processing` (a requeued task fails with `batch already failed`). (3a, partial) `frontend/src/money.js` `formatMoney` (string in, digits handled with BigInt, never `parseFloat`; cents always; thousands commas; parentheses for negatives; `0.00` for zero; half-up) with `node --test` unit tests (`npm test`, no dependency, in CI and `make test`); a static check that nothing under `frontend/src` calls `parseFloat` or `Number(`; the Imports page stacks to cards and the Shell header wraps below 640 px so 390 px has no sideways page scroll. (3b) `latest_successful_cursor(db, connection_id, kind)` returns the cursor of the latest *succeeded* run; `iso_utc()` makes ISO-8601 UTC strings and `finish_sync_run` refuses a `datetime` inside a cursor. (4) `scripts/create_user.py create-tenant` and `add-entry` (same audited admin service, actor `cli`; `admin.create_tenant` accepts the CLI actor); OPERATIONS.md "Rebuild local dev data" with the four commands in order; no command sets a password (D-16). OPERATIONS.md also states that append-only triggers do not stop `DROP TABLE` by the schema owner and that backups (F23) are the protection.
**Why**: Owner review of F03 (2026-09-18), after the dev database was emptied by a round-trip run during close-out; retrying a deterministic parse failure five times was waste; a failed batch must be final so raw history stays honest; the rebuild sequence needed to be four commands, not a curl session with TOTP.
**Tests**: 349 backend (338 + 11) and 5 frontend (`node --test`). New: downgrade guard (protected name, missing flag, default `wip`, scratch with the flag downgrades; `make downgrade` refuses without the flag), parse fail-fast, transient object-open and database failures retrying with the batch left `received`, a failed batch never reopening (no `processing` write observed), `latest_successful_cursor`, `iso_utc`, cursor `datetime` refusal, CLI `create-tenant` / `add-entry` audited with actor `None`, `parseFloat` static check; `formatMoney` cases incl. `141366.68000000002`, `2.675` half-up, `-0.00`.
**Not received**: the D-22 text and the CLAUDE.md "Interface conventions" section (3a) were never pasted; `docs/DECISIONS.md` and CLAUDE.md carry neither yet.
**Migrations**: none. **Decisions referenced / made**: D-13, D-15, D-16, D-19, D-20; D-22 pending text. **Dependencies added**: none.
**Follow-ups**: owner browser pass (unticked); the F03 brief's "task stops at max_attempts" wording for parse errors is superseded by this entry.
**Correction (2026-09-18, D-22)**: the "Interface conventions" text *had* been received: it was appended to the end of CLAUDE.md in the working tree and went into commit d0b08d1 unnoticed. D-22 is now in `docs/DECISIONS.md` and the section sits after "Code conventions" (commit "D-22 · interface conventions"). Imports-page deviations from the section are listed in that commit's message and under Discovered in `current-feature.md`.

---

## 2026-09-17 · F03 · Ingestion framework
**What**: Five tenant tables in migration 0004 (`connection`, `sync_run`, `import_batch`, `raw_record`, `task`; RLS enabled and forced on each, leading index, `raw_record` append-only by trigger and listed in `APPEND_ONLY_TABLES`, partial unique index on `task (tenant_id, kind, dedupe_key)` for non-terminal rows). One JSON codec for every JSON/JSONB column (`app/core/jsoncodec.py`, installed at the engine and, through SQLAlchemy's psycopg dialect, at the driver): `Decimal` written as a JSON number with its digits by an encoder of our own, `float` and non-finite Decimals refused naming the key path, `parse_float=Decimal` on read, `payload_sha256` over the canonical form before storage. Raw store (`app/ingest/raw.py`): `store_raw` versions by payload hash, a source delete is a flagged version carrying the last payload, prior versions never change. Object storage behind a protocol (`app/core/storage.py`): `LocalObjectStore` and `S3ObjectStore` (Spaces via boto3, private ACL, signed URLs); the store builds the `tenant/{tenant_id}/` prefix, refuses `..`, a leading `/`, a backslash and anything outside `[A-Za-z0-9._-/]`. Source registry (`app/integrations/base.py`: `SourceKind`, `RawItem`, `RejectedItem`) with one production kind, `unparsed_file`; the test-only CSV kind lives in `tests/csv_source.py`. Upload pipeline (`app/ingest/imports.py`, `app/api/imports.py`): `POST /api/imports` streams to a temp file while hashing, refuses over `MAX_UPLOAD_BYTES` (413), a wrong extension (415) or an unknown kind (422) before storing; writes the content-addressed object `imports/{sha256}.{ext}` before the row commits; row, task and `import_uploaded` audit land in one transaction; the same bytes for the same tenant and kind return the existing batch with `duplicate: true` (200, `import_duplicate`); a store failure is 503 and leaves no row; `GET /api/imports`, `/{id}`, `/{id}/download` (stream for local, 307 to a signed URL for S3, `import_downloaded`); another tenant's batch is 404. Task `import.process_batch`: parse → `store_raw` per item under a savepoint; a `RejectedItem` or a store-refused item counts in `rows_rejected` and the batch ends `loaded_with_issues`; a raising `parse` marks the batch `failed` with `parse failed: <Type>` and re-raises for the queue's attempts. Worker (`app/worker/`): `@task` decorator refusing a first parameter other than `tenant_id`; the loop reads the tenant list and for each tenant claims at most one due task in one `tenant_session` (`FOR UPDATE SKIP LOCKED`), runs it, and completes or fails it in a third transaction **conditional on still holding the lease** (a reclaimed worker writes nothing, logs `outcome=lease lost`); lease 300 s, no heartbeat, 5 attempts, backoff 30 s × 2^(n−1) capped at 15 min, sweep of expired leases at the limit, warning past 80% of the lease; `LISTEN wip_tasks` wakes an idle worker, polling is the fallback; `python -m app.worker`, `make worker`. Connection tokens (`app/ingest/connections.py`): AES-GCM under the key ring with associated data `tenant_id|connection_id|field` (`aad_for`), audited `connection_tokens_set` without values; `access_token`, `refresh_token`, `payload` added to the forbidden audit-detail keys. `scripts/reencrypt.py` (`make reencrypt`): moves `user.totp_secret_enc` and connection tokens to the active key, one transaction per tenant, idempotent, counts by key id, exits 1 on an undecryptable blob. Capability `can_manage_imports` (firm_admin, firm_staff, client_admin). Frontend: `pages/Imports.jsx` (source kind, file, upload with the submit disabled in flight, duplicate notice, list with status, counts, uploader e-mail, time, download link, manual Refresh) reached from a nav in `Shell.jsx` for those roles; `upload()` in `api.js`. Housekeeping: CI Node 20 → **24** (current LTS; `frontend/.nvmrc`), CI refuses tracked files under `.object-store`, `.gitignore`, `.env.example`, `Makefile` (`worker`, `reencrypt`), OPERATIONS runbooks (worker, stuck/failed task and requeue, object store, imports, re-encrypt), README "Adding a source kind".
**Why**: BLUEPRINT §3 principle 4 (raw first), §6.1, §11, §12; D-19 (tenant-scoped queue, one tenant per transaction), D-20 (`raw_record` insert-only, versioned), D-21 (object store behind a protocol, tenant prefix built by the store). The owner's conditions on the plan calls: a Decimal must reach storage as the JSON number QBO sent (stdlib `json` cannot emit one, so the structure is encoded here), the codec must be the same at the driver level so raw SQL cannot disagree, and a late write from a worker that lost its lease must change nothing.
**Tests**: 338 pass (231 + 107). New files: `test_jsoncodec.py`, `test_raw_store.py`, `test_storage.py`, `test_imports.py`, `test_worker.py`, `test_connections.py`, `test_reencrypt.py`; extended: `test_rls.py` (each F03 table reads zero rows of another tenant via ORM and raw SQL as `app_rw`), `test_append_only.py` (`raw_record` for both roles), `test_migrations.py` (0004 alone reversible), `test_roles.py` (five import routes incl. a multipart cell), `test_hygiene.py` (`json.loads` only in the codec with `parse_float`; no `float(` on ingestion paths; no `Principal` / `active_tenant_id` / `firm_ids` under `app/worker/` or in task modules; `.object-store` untracked), `test_zz_log_leaks.py` and `test_zz_response_scan.py` (connection tokens). **Mutation checks done by hand, each reverted**: (a) `actor.active_tenant_id` appended to `app/ingest/imports.py` → the worker-isolation test fails naming the line; (b) `from app.core.auth import Principal` appended to `app/worker/queue.py` → fails naming `Principal`; (c) `json.loads("{}")` appended to `app/ingest/raw.py` → the codec test fails naming the line. `npm run build` passes; `test_frontend_effects.py` passes with no new allow-list entry. `alembic upgrade head → downgrade base → upgrade head` passes with the crypto variables unset. The worker process was started and stopped with SIGTERM by hand. The owner browser-pass criterion stays unticked.
**Migrations**: 0004 (five tables; reversible, drops them). **Decisions referenced / made**: D-19, D-20, D-21 (staged in `DECISIONS_D-19_to_D-21.md`, owner appends). **Dependencies added**: backend `boto3`, `python-multipart` (approved in the brief).
**Follow-ups**: see Discovered in `docs/briefs/F03.md`: money in API responses must be strings (F08); migration round-trip checks only on a scratch database (the local dev database `wip` was emptied by one run during close-out, re-create dev users with `scripts/create_user.py`); `sync_run` / `connection` get routes in F05. Before the first deployment: create the Space and keys and record the first manual upload/download in OPERATIONS.md (D-21).

---

## 2026-09-17 · F02.1 · owner pass and enrolment auto-select
**What**: The owner's browser pass is done (Chrome, 2026-09-17): activation link with the token cleared from the address bar, password, QR identical after a refresh, code accepted, recovery codes, sign-in with password and code, company switch, sign-out, spent link refused. Ticked on the owner's instruction, each noted "owner pass, 2026-09-17": the last criterion in `docs/briefs/F02.1.md` and the frontend criterion in `docs/briefs/F02.md`. Gap found in the pass and fixed: the single-company rule now lives in one function, `auto_selected_tenant()` in `app/auth/service.py`, called by password login and by `confirm_totp_enrolment`; at confirm it sets the tenant on the enrolment-only session before the rotation, so `_complete_login` writes `tenant_enter` (`via: auto`) to that tenant's `audit_log` in the same transaction and `login_success` carries the tenant.
**Why**: A user with exactly one company was auto-selected at sign-in but landed on "Choose…" after enrolling through a link, because the link's session starts with no tenant and the rotation copied that.
**Tests**: 231 pass (229 + 2): a firm user with one entry row has `active_tenant_id` set in the confirm response and in `/me`, with exactly one `tenant_enter` row (`via: auto`, `firm_staff`) and the tenant in `login_success`; a user with two entry rows lands with none selected and no `tenant_enter` row. The first was red before the change (`active_tenant_id` was `None`); the second passed before and after, and pins the rule's other half.
**Migrations**: none. **Decisions referenced / made**: none. **Dependencies added**: none.
**Follow-ups**: one new Discovered note in `current-feature.md` (a client user's optional enrol confirm records a second `login_success`).

---

## 2026-09-17 · F02.1 · enrolment follow-ups
**What**: `backend/tests/test_frontend_effects.py`: a static check that fails when a `useEffect` / `useLayoutEffect` body in `frontend/src` calls `api(` with a method other than GET, directly or through a function defined in the same file, unless the file is in `GUARDED` (only `pages/TotpEnrol.jsx`, with the reason). Comments are ignored; `api(` with a non-literal method and `fetch(` with a `method` option inside an effect are refused rather than interpreted; a stale `GUARDED` entry fails; the entry's guard (request held in a ref, StrictMode still on) is asserted. It does not follow helpers imported from another module. `Activate.jsx` disables its submit button while the request is in flight, like Login, TotpVerify and the TotpEnrol confirm form, which already did.
**Why**: The enrolment bug came from a POST in a mount effect, and nothing automated looked at effects. A double click on the activation form posted the single-use token twice and showed "invalid link" over a success.
**Tests**: 229 pass (222 + 7): four on the scanner itself (the original enrolment effect is flagged; GETs, a commented-out call and requests in handlers pass; same-file functions are followed; unreadable calls are refused), three on the tree. **Mutation checks done by hand, each reverted**: (a) the `TotpEnrol.jsx` entry removed from `GUARDED` → `test_no_effect_sends_a_non_get_request_outside_the_guarded_list` fails naming `pages/TotpEnrol.jsx` line 21 `api("POST")`; (b) an effect calling `switchTenant()` added to `Shell.jsx` → the same test fails naming `switchTenant() → api("POST")`. `npm run build` passes. The owner browser-pass criterion stays unticked.
**Migrations**: none. **Decisions referenced / made**: none. **Dependencies added**: none.

---

## 2026-09-17 · F02.1 · enrolment idempotency fix
**What**: `POST /api/auth/totp/enrol` is idempotent while an enrolment is pending: it locks the user row (`SELECT … FOR UPDATE`, re-read under the lock) and returns the stored unconfirmed secret, generating one only when none is pending; already enrolled is still 409. A pending secret (secret present, `totp_enrolled_at` NULL; no schema change) is dropped by `_clear_pending_totp()` when a new activation link is issued, when a link is redeemed, and at a successful password login (a client user starts optional enrolment from a normal session, so the next login is the "later enrolment session"); admin TOTP reset already cleared it; confirm turns it into the enrolled secret. `TotpEnrol.jsx` keeps its one enrol request in a ref, so StrictMode's doubled mount effect sends one request; StrictMode stays on.
**Why**: Owner browser pass. React 18 StrictMode ran the mount effect twice in dev, two concurrent enrol calls each wrote a new secret, the server kept the last write and the screen showed either one, so codes never matched. A refresh, a double click or a retry did the same in production. The F02 end-to-end check was the API flow through the Vite proxy with one call per step, and every backend test called enrol exactly once, so no check ever made the second call.
**Tests**: 222 pass (217 + 5): two sequential enrol calls return the same secret and URI, the secret is stored as key-ring ciphertext, a code from the first response confirms, and the same ciphertext becomes the enrolled secret; two concurrent calls return the same secret (the test holds the user row lock until both requests queue on it; with `FOR UPDATE` removed the test fails with two different secrets, checked by hand and reverted); issue-link and admin TOTP reset clear a pending secret at once and the next enrolment gets a different one; redemption clears on its own; a client user's abandoned secret is gone at the next login. Response scan and log capture pass unchanged. `npm run build` passes. The owner browser-pass criterion stays unticked.
**Migrations**: none. **Decisions referenced / made**: D-16. **Dependencies added**: none.
**Follow-ups**: see "Discovered" in `current-feature.md` (pending secret is per user, not per session; `Activate.jsx` has no double-submit guard; proposed static check for non-idempotent requests in mount effects).

---

## 2026-09-17 · F02.1 · CI first-run fix
**What**: `alembic/env.py` reads `MigrationSettings` (`DATABASE_OWNER_URL` and nothing else) instead of the full `Settings`; no migration uses the keys. The test harness is hermetic: `tests/__init__.py` imports `tests/_env.py` before anything imports the application (and `_env.py` refuses to load otherwise); it generates a throwaway key ring with `os.urandom` per run, points `ENV_FILE` at a missing path, and `conftest` drops every other `Settings` variable from the environment. The fixed test keys are gone. `ci.yml`: checkout, setup-python and setup-node moved to v7; no crypto key is set because no step needs one.
**Why**: The first real CI run failed at the migration step with `missing required environment variable(s): CRYPTO_ACTIVE_KEY_ID, CRYPTO_KEYS`, because `env.py` built the full `Settings`. The Tests step would have failed next: an import sort had moved `tests._env` below the application imports in `conftest`, and `app.main` builds `Settings` at import, so the suite was reading the developer's `.env` and passed locally only because that file existed.
**Tests**: 217 pass (214 + 3): alembic up/down as a subprocess with only `DATABASE_OWNER_URL`; its absence names that variable alone; the suite's env file does not exist and the wrong import order is refused. 217 pass with `.env` renamed (before the change: exit with the CI message). CI reproduced locally in a clean shell (empty Postgres, fresh clone and venv, only `ci.yml`'s variables), every backend step green. No key-shaped string in any tracked file.

## 2026-09-17 · F02.1 · Auth hardening patch
**What**: Firm authority is an explicit row (D-15): new global table `firm_membership(firm_id, user_id, role ∈ {firm_admin, firm_staff})`, unique per firm and user, CHECK-constrained to firm roles. `membership.role` becomes nullable with a CHECK allowing only NULL or a client role: a NULL row is an *entry row* that lets a firm user open that tenant with the authority of their `firm_membership`. Exactly one function, `app.core.auth.effective_role`, computes the role that applies inside a tenant; a static test proves nothing else reads `membership.role`. An entry row whose user has no `firm_membership` in the tenant's firm is an orphan and grants no access (403 on switch, absent from the tenant list). `firm_admin` routes for firm memberships (`GET/POST /api/admin/firm-memberships`, `PUT/DELETE …/{user_id}`) audited to `firm_audit_log`; the last `firm_admin` cannot be removed or demoted, with the firm row locked `FOR UPDATE` before counting (two concurrent demotions leave one admin and one 409). Removing a firm membership commits first (row, audit, sessions), then removes entry rows one tenant per transaction (owner answer A; a simulated failure part-way leaves no access and a retry finishes). `POST /api/admin/tenants` creates a bare tenant in the caller's firm (owner answer C). A user holds either a firm role or client roles in one firm, never both; the API refuses the mix. Accounts activate only through an admin-issued link (D-16): users are created with no password; `POST /api/auth/activate` sets it and, for a firm user without TOTP, opens a 15-minute enrolment-only session (`ENROL_SESSION_TTL_MINUTES`) in which the existing enrol/confirm routes finish the job and show the recovery codes; a firm user without TOTP cannot log in by password (generic failure); issuing a link voids the earlier one and ends sessions; admin TOTP reset and password reset both return a new link; `/api/auth/password/reset` and the `password` field on user creation are gone; the `password_reset_*` columns are renamed `activation_*`. Every session of a user ends on password change (the caller's too), link redemption, TOTP reset, and any `firm_membership` change; removing a membership clears `active_tenant_id` and keeps the session. Per-IP throttle (`app/core/throttle.py`) on login, TOTP verify and recover, and activation, counted from `firm_audit_log`: 20 failures in 15 minutes → one `ip_throttled` row, 429 for the rest of the window, nothing else written; the check runs before any argon2 verification; new index on `firm_audit_log(ip, occurred_at)`. Client IP from `X-Forwarded-For` only with `TRUSTED_PROXY_COUNT` > 0 (N-th from the right; socket address when the header is missing or short). Unknown-e-mail failures store `email_sha256`; the `email` key is refused in audit detail. Null-firm audit rows are shown to `firm_admin` only (D-17). Every user-bearing response goes through an allow-list schema (`app/api/schemas.py`); credential columns on `user` are a deferred group loaded only by the auth service; a suite-wide scan checks every JSON body for the credential column names and stored values. `Settings`: `DATABASE_URL`, `CRYPTO_KEYS`, `CRYPTO_ACTIVE_KEY_ID` have no defaults and the process exits naming the missing variable; `DATABASE_OWNER_URL` is optional and required only by Alembic; `ENV_FILE` selects the env file. `.env.example` holds no key material; CI fails if `.env` is tracked; ruff pinned to 0.16.8 in the dev dependencies and CI. The middleware refuses preflights and state-changing requests whose `Origin` differs from `APP_BASE_URL` (no `Access-Control-*` headers ever). The probe router moved to `tests/probes.py`; the application package no longer mentions pytest. The production engine is built with `hide_parameters=True` and `echo=False` (the harness overrides it to keep parameter capture), and a `DBAPIError` handler logs SQLSTATE plus the primary message only. D-18: `read_as_user()` in `app/core/auth.py` is the single way to read another user's `membership` rows (swap `app.user_id`, restore in `finally`, callable only from `app/auth/admin.py`, results filtered to the actor's firm). `scripts/create_user.py` gained subcommands `bootstrap` (firm + first `firm_admin`, no tenant needed), `create-user`, `issue-link`, `reset-totp`; it prints the link once to stdout and never sets a password. Frontend: `Activate.jsx` replaces the reset-password page and continues into TOTP enrolment. Docs: CLAUDE.md amendments (tenant-scope naming rule, approved global tables, data migrations never unforce RLS, the D-18 `read_as_user()` sentence, one live brief, commit-at-close-out rule), closed briefs moved to `docs/briefs/`, D-15…D-18 appended to `docs/DECISIONS.md`, OPERATIONS runbooks for bootstrap, links, throttle and `TRUSTED_PROXY_COUNT`.
**Why**: The F02 review found that a wrong role on one client granted firm-wide administration, that a stolen password alone could enrol the second factor, that credential changes left sessions alive, that anonymous callers could grow an insert-only table without bound, and that nothing but discipline kept credential columns out of responses. D-15, D-16 and D-17 settle the model while the auth tables hold no real users. The owner's conditions on option (b) (one role function, orphan rows = no access), on removal (no task iterates tenants inside one transaction), on the data step (never unforce RLS), and on the swap helper (D-18) are recorded in the F02.1 brief. The idle-timeout bookkeeping in `get_principal` moved to its own short transaction because holding a row lock on the session for the whole request deadlocked with an admin ending that user's sessions concurrently.
**Migrations**: `0003_firm_membership_activation` (reversible; verified up → down → up on a scratch database, alone and with 0002, and on the local `wip`). Creates `firm_membership`, renames the two activation columns, adds the IP index, and runs the data step per tenant with `app.tenant_id` set: one `firm_membership` per (firm, user) with the highest F02 role, firm-role membership rows become entry rows, abort with the user ids if anyone holds a firm role and a client role in one firm. Downgrade restores roles from `firm_membership`; orphan entry rows (which grant nothing) are the only lossy part and are reported with a NOTICE.
**Tests**: 214 passing (F02: 150). New: `test_firm_membership.py` (roles from `firm_membership`, CHECK constraints, entry-row flow with `membership_created` then `tenant_enter`, mutual exclusion, last-admin guard and atomic audit, concurrent demotions with the firm row locked, orphan rule, two-phase removal with simulated failure and retry, bootstrap on an empty database through to tenant creation and entry), `test_activation.py` (no password at creation, single use, expiry, firm flow with 15-minute session and gated routes, TTL, supersession, TOTP reset, no password-only enrolment, session invalidation matrix, CLI link printed once and never logged), `test_throttle.py` (21st failure 429, one row, 100 further attempts add nothing, no argon2 when throttled, link failures count, forged header ignored, proxy count 1 and 2, missing or short header), `test_hygiene.py` (e-mail hashed, null-firm rows for firm_admin only, start-up refusal naming the variable, `.env` untracked and example key-free, origin policy, no pytest in the app, probes 404 normally, production engine options, IntegrityError without bound values, D-18 helper restore and write behaviour, single call site, single role-read site), `test_zz_response_scan.py` (names, stored values, tokens only in `activation_url`, never in audit detail). Changed: role matrix (+7 routes, 3 scratch states), auth, admin, audit, RLS (entry row assertion), migrations (0003 data step and refusal), log leaks (attempted e-mail against application records, activation tokens verbatim). **Mutation checks done by hand, each reverted**: (a) a second read of `Membership.role` outside `effective_role` (in `change_membership_role`) → `test_membership_role_is_read_only_inside_effective_role` fails; (b) a second call site for `read_as_user()` (in `redeem_activation_link`) → `test_read_as_user_is_called_only_from_the_admin_service` fails; (c) `password_hash` added to the `UserOut` response schema → `test_no_credential_column_name_in_any_json_body` fails. Post-close additions (owner, 2026-09-17): the activation token travels in the URL fragment (`/activate#token=…`), which browsers never send to a server; the page reads it, clears the address bar with `history.replaceState`, and posts it in the body; a test asserts no backend route declares a query parameter named like a token and that a token in the query string is ignored. `get_principal` reads the session row, the user, memberships and firm memberships inside the request transaction; only the `last_seen_at` write is a separate short transaction, and it happens only when the value is at least `SESSION_TOUCH_SECONDS` (60) stale; tests: a session ended by an admin is 401 on its very next request, a fresh session is not rewritten and a stale one is, and the idle and absolute expiry tests still pass. 214 tests.
**Decisions referenced / made**: D-15, D-16, D-17 (owner, staged file appended), D-18 (owner constraints, written here). Corrects the F02 follow-up: D-15 does **not** make `firm_audit_log.firm_id` NOT NULL; unknown-e-mail failures, failed link redemptions and IP throttles have no firm.
**Dependencies added**: none.
**Follow-ups**: owner browser pass through the CLI (closes the F02 and F02.1 frontend criteria); admin UI for users, memberships and links (Discovered, not built); re-encrypt command for key rotation arrives with F03; Postgres echoes the conflicting key in an `IntegrityError` DETAIL line, so what the application logs about a database error is limited to SQLSTATE and the primary message (`describe_db_error`).

---

## 2026-09-17 · F02 · Auth, roles, audit log
**What**: Hand-rolled authentication (D-10): e-mail + argon2id password (12+ chars), 5-failures-in-15-minutes lockout with identical "unknown e-mail" / "wrong password" responses, TOTP (RFC 6238, ±1 step, stored time-step counter so a code cannot be replayed) mandatory for `firm_admin` / `firm_staff` and optional for client roles, ten hashed one-time recovery codes, `firm_admin` password-reset links (returned to the admin, no e-mail) and TOTP resets, and `scripts/create_user.py` for bootstrap and last-resort resets. Server-side sessions in the new global `session` table (256-bit id in an `HttpOnly; Secure; SameSite=Lax` cookie, only the SHA-256 stored; id rotates at login and at TOTP verification; idle 60 min and absolute 12 h from environment; logout deletes the row). CSRF middleware requires `X-Requested-With` on every `POST/PUT/PATCH/DELETE` under `/api/`. Tenant context now comes only from the session: `require_tenant_id` reads the principal, the `X-Tenant-Id` header is gone; `get_principal` sets `app.user_id` and `app.tenant_id` with `set_config(..., true)` on the one request transaction. D-11 own-membership `FOR SELECT` policy via `allow_own_membership_read()`, and `tests/test_rls.py` now allow-lists extra policies (exactly one). Tenant switcher (`GET /api/session/tenants`, `POST /api/session/tenant`), auto-selection for single-membership users. `app/core/authz.py`: `require_roles` / `require_firm_role` and named capabilities (`can_view_pay_rates` = firm_admin, firm_staff, client_admin, …); a test-only probe router mounts one route per capability under pytest. `firm_admin` endpoints for users, memberships (in any tenant of the firm, including one with no members yet), resets. Audit (D-12, D-13): tenant-scoped `audit_log` (RLS) and global `firm_audit_log`, both insert-only via `make_append_only()` triggers and registered in `APPEND_ONLY_TABLES`; plain JSON read routes `GET /api/audit` (firm roles + client_admin) and `GET /api/firm-audit` (firm roles). `app/core/crypto.py`: AES-256-GCM key ring with key id stored beside the ciphertext, used for TOTP secrets. Frontend: login, TOTP verify (with recovery-code option), TOTP enrolment (QR + manual key, recovery codes shown once), reset-password page, shell with tenant switcher and sign-out; unauthenticated users see only the login page. Housekeeping from the F01 review: compose host port from `POSTGRES_HOST_PORT` (default 5433); this CHANGELOG template gained **Tests** and **Dependencies added**. Also fixed: `Settings` now reads the repo-root `.env` when run from `backend/` (F01's defaults had hidden that it was never loaded).
**Why**: BLUEPRINT §3.8 and §11: every request is tied to an authenticated user, a role, and a tenant taken from the server-side session, never from the client. Firm-level authority is **derived from memberships** (no schema change; see the F02 plan for the `firm_membership` proposal, D-15 candidate). Expected failures that must be persisted (failed login, failed code, lockout) are returned as results and rendered as 401/429 so the request transaction commits; failures that must roll back (403 on tenant switch) raise. `session` stores the active tenant as `active_tenant_id` so the RLS enumeration does not mistake the global table for a tenant table. `firm_audit_log.firm_id` is nullable because an unknown-e-mail failure has no firm.
**Migrations**: `0002_auth_audit` (reversible; verified up → down → up on a scratch database in a test and on `wip` via the CLI). Adds credential columns to `user`; creates `session`, `audit_log` (RLS + append-only), `firm_audit_log` (append-only); creates the `own_membership_read` policy and the `raise_append_only()` trigger function.
**Tests**: 150 passing (F01: 25). New: role matrix (5 roles × 22 protected routes, plus 401 unauthenticated on each), pay-rate probe, TOTP gate for unenrolled and unverified firm sessions, replay, recovery code single use, lockout on the sixth attempt with identical bodies, `X-Tenant-Id` ignored, own-membership read via ORM and raw SQL (zero rows without `app.user_id`; insert rejected, update/delete match nothing with it), tenant switch without membership 403 and unchanged, extra-policy allow-list, `audit_log` isolation, append-only for `app_rw` and `app_owner` including drop/disable attempts, audit rows and atomicity of role change + audit row, firm audit readable by firm roles only, cookie flags and hash-only storage, rotation, idle and absolute expiry, logout, CSRF, key rotation, crypto ring, and a suite-wide log capture (SQLAlchemy statement logging on) that finds no password, secret, code, recovery code, or session id. **Mutation checks done by hand**: a `sneaky` policy on `_rls_probe` and a second policy on `membership` each make the enumeration test fail; a registered append-only table without triggers fails (in-process test). End-to-end against the local stack through the Vite proxy: login → enrol (QR/secret) → confirm → tenant list → admin joins a tenant without prior membership → switch → audit reads → logout → replayed code refused; cookie flags as specified. The React screens were not driven in a browser in this session (no browser automation available); `npm run build` passes.
**Decisions referenced / made**: D-10, D-11, D-12, D-13, D-14 (appended to `docs/DECISIONS.md`). Proposal for D-15 (`firm_membership`) recorded in `current-feature_02.md`.
**Dependencies added**: backend `argon2-cffi`, `pyotp`, `cryptography`; frontend `qrcode` (all pre-approved by D-10/D-14).
**Follow-ups**: owner decision on D-15 (firm authority table; makes `firm_audit_log.firm_id` NOT NULL); re-encrypt command for key rotation arrives with F03 token storage; a firm user without TOTP who holds a stolen password can enrol their own TOTP (inherent to the brief's enrolment rule; mitigated by admins creating users with reset links and watching `totp_enrolled`); browser pass of the React flow by the owner. See "Discovered" in `current-feature_02.md`.

---

## 2026-09-17 · F01 · Project skeleton & tenant isolation
**What**: Repo layout per BLUEPRINT §12 (`backend/`, `frontend/`, `docs/`, `db/`, `tests/fixtures`, `tests/golden`); `docs/BLUEPRINT.md` moved from the repo root. Docker compose Postgres 16 on port 5433 with a bootstrap script that creates `app_owner` (schema owner, migrations) and `app_rw` (application; not owner, `NOSUPERUSER`, `NOBYPASSRLS`) and grants `app_rw` DML through default privileges so migrations never name the role. FastAPI app with `/api/health`, config from environment, and a request-scoped session dependency that opens a transaction and sets `app.tenant_id` with `set_config(..., true)` (`SET LOCAL`). `tenant_session(engine, tenant_id)` context manager for workers and scripts. Alembic baseline with `firm`, `tenant`, `user`, `membership`, and the test-only `_rls_probe`. Migration helper `enable_tenant_rls(table)` that enables **and forces** RLS and creates the `tenant_isolation` policy. React + Vite shell that calls the health endpoint. GitHub Actions CI: ruff, migration up/down/up, pytest, frontend build. README and OPERATIONS runbooks.
**Why**: BLUEPRINT §3.8 and §11: isolation is enforced by the database, not by remembering a `WHERE` clause. Doing it in the first migration means every later table inherits the guarantee and the CI test (`tests/test_rls.py`) enumerates every `tenant_id` table so a missed helper call fails the build. Policy uses `NULLIF(current_setting('app.tenant_id', true), '')::uuid` so a missing or expired setting yields zero rows and rejected writes rather than a cast error.
**Migrations**: `0001_baseline` (reversible; downgrade drops the five tables and the `membership_role` enum). Verified up → down → up on an empty database in a test and via the CLI.
**Tests**: 25 passing. RLS enabled+forced+policy+leading index on every `tenant_id` table; cross-tenant reads return nothing via ORM and raw SQL as `app_rw`; cross-tenant inserts and updates are rejected; no context → zero rows, insert fails, also for the owner (FORCE); `app_rw` cannot disable/unforce RLS or drop the policy; context does not leak across pooled transactions; request dependency scopes correctly per header; product name appears in exactly one constant per side. Mutation check done by hand: an unprotected `tenant_id` table makes the enumeration test fail.
**Decisions referenced / made**: none. D-09 (name) still open; working name lives in `backend/app/core/product.py` and `frontend/src/product.js` only.
**Dependencies added**: fastapi, uvicorn, sqlalchemy, alembic, psycopg[binary], pydantic-settings; dev: pytest, httpx, ruff. Frontend: react, react-dom, vite, @vitejs/plugin-react.
**Follow-ups**: F02 replaces the `X-Tenant-Id` header placeholder in `require_tenant_id` with the authenticated tenant. See "Discovered" in `current-feature.md` for the rest.

---

## 2026-09-17 · Project inception
**What**: BLUEPRINT, ROADMAP, CLAUDE.md, and F01 brief created from review of Rye Beach Landscaping's chart of accounts, balance sheet, P&L (YTD 9/17/2026), and two LMN estimator closing reports.
**Why**: Establish design before code. Key constraints found: LMN has no general API (file import); QBO Projects GraphQL API is partner-gated (use core REST API); Ramp passes QBO projects through its Customer/Job field (no Ramp integration needed in v1); payroll posts to the GL in lump sums (labor job cost must be computed from timesheets).
**Decisions referenced / made**: D-01…D-09 open; see BLUEPRINT §14.
**Follow-ups**: Phase 0 checklist (BLUEPRINT §13).
