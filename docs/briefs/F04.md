# current-feature.md

## F04 · Tenant configuration
**Roadmap phase**: A · **Blueprint refs**: §5 (configuration tables), §8.1 (WIP basis), §8.5 (tie-out), §11 · **Decisions**: D-22, D-23
**Status**: built (2026-09-18), 423 backend + 7 frontend tests; owner browser pass pending (last acceptance criterion, not ticked by Claude Code).

### Goal
Each tenant can describe its own books to the platform: its divisions, which ledger accounts are job cost and of what kind, its burden rates, and its accounting policy settings. Rye Beach's chart of accounts loads from a file and the platform proposes the mapping from the account-number scheme; a person confirms it. Nothing here reads QuickBooks, and no accounting policy is given a default value.

### In scope
**Tables** (all tenant-scoped: `tenant_id NOT NULL`, leading index, `enable_tenant_rls`)
- `division`: `code` (LS, EX, GC, SNOW, …), `name`, `code_digit` (one digit, nullable, unique per tenant when set: Rye Beach LS 1, EX 2, GC 3, SNOW 4), `active`, `sort_order`. Unique `(tenant_id, code)`. Not seeded globally: divisions belong to the tenant.
- `cost_category` (D-23): seeded per tenant with exactly the D-23 list, each with a two-digit `slot` (10 Labor, 20 Labor Burden, 30 Materials, 35 Supplies, 40 Subcontractors, 45 Equipment (owned), 47 Vehicles (owned), 50 Equipment Rental, 55 Equipment Maintenance, 60 Disposal, 65 Fuel, 70 Permits & Bonds, 80 Warranty, 90 Other). `active`, `sort_order`. Categories are deactivated, never deleted. Adding a category beyond D-23 needs a new decision. CLAUDE.md Vocabulary is updated to this list verbatim.
- **Cost code** (D-23) is not a table: it is the division's `code_digit` followed by the category's `slot` (SNOW Labor = 410), computed by one function and shown wherever a division and a cost category appear together. For Rye Beach it equals the ledger account number without its leading 5 (5410), which is what the suggest rules exploit.
- `gl_account`: the tenant's chart of accounts as the platform knows it: `account_no`, `name`, `ledger_type` (text as the source gives it), `active`, `source` (`file` now, `qbo` in F05), `external_id` (nullable until F05), `raw_record_id`. Unique `(tenant_id, account_no)`. F05 will attach QBO ids by account number, which is an identifier, not a name.
- `account_map`: one row per mapped `gl_account`: `division_id` (nullable: shared accounts have none), `cost_category_id` (nullable: income accounts have none), `in_job_cost` (boolean, NOT NULL), `status ∈ {suggested, confirmed}`, `suggested_by_rule`, `confirmed_by`, `confirmed_at`. Only `confirmed` rows are used by any later feature.
- `account_suggest_rule`: the tenant's numbering scheme as data, not code: ordered rules of the form "account number matches pattern → division / cost category / in_job_cost". Rye Beach's rules are loaded from a fixture; a tenant with no rules simply gets no suggestions (F20's second tenant uses Classes).
- `tenant_policy`: typed keys, one row per key, with `value` (JSONB through the F03 codec), `decided_by`, `decided_at`, `decision_ref`. Keys created in F04: `timezone`, `fiscal_year_start_month`, `wip_basis` (set of cost categories), `small_job_threshold`, `deposit_identification`, `fuel_surcharge_treatment`. **Accounting-policy keys have no default.** An unset key reads as "not decided", and a helper `require_policy(key)` raises a clear error that later features (F08, F14) will surface rather than guessing.
- `burden_rate`: effective-dated (`effective_from` as `date`), optional `division_id`, `rate` as `NUMERIC(7,4)` (a fraction, Decimal end to end), `basis_note`. No overlapping periods for the same division.

**Chart of accounts import**
- A new source kind `chart_of_accounts` registered with the F03 source registry: accepts `.csv` and `.xlsx`, yields one `RawItem` per account with `external_id = account_no`. XLSX cells are read as text or converted string → Decimal; never through `float`. Rows without a four-digit account number are `RejectedItem`s (section headings, the scheme legend at the bottom of Rye Beach's workbook); the batch ends `loaded_with_issues` and the rest loads.
- A normalizer (idempotent, re-runnable) turns the latest `raw_record` per account into `gl_account` rows: new accounts are inserted, renamed accounts are updated with the old name kept in the audit detail, accounts missing from a newer file are marked inactive, never deleted.
- After normalizing, the suggestion step writes `account_map` rows with `status = suggested` for accounts that match a rule and have no confirmed mapping. It never overwrites a confirmed row.

**Screens** (D-22 conventions; one stylesheet; table pattern from F03.1)
- **Accounts**: every account with number, name, type, division, cost category, in job cost, status. Filter: all / unmapped / suggested / confirmed. Edit one row; "Confirm all suggestions" as the one primary action, with a count and a confirmation step. **Unmapped accounts are always listed and counted at the top.** The table scrolls in its own container with the account column held in place.
- **Divisions**, **Cost categories**, **Burden rates**: plain list and add/edit/deactivate forms. A read-only **Cost codes** view shows the division × cost category grid with each code and the ledger account mapped to it, or "no account" where none is.
- **Policy**: each key with its value or "Not decided", who decided, when, and the decision reference. Editable by `firm_admin` only.
- Read access: `firm_admin`, `firm_staff`, `client_admin`. Write access: `firm_admin`, `firm_staff` (capability `can_manage_tenant_config`); policy keys `firm_admin` only. `client_pm` and `client_viewer` get 403.

**Audit**
- Every create, change, confirm, deactivate on `division`, `cost_category`, `account_map`, `account_suggest_rule`, `tenant_policy`, `burden_rate` writes to `audit_log` in the same transaction, with before and after values. "Confirm all suggestions" writes one row per account, not one summary row.

**Fixtures**
- `tests/fixtures/rye_beach/chart_of_accounts.csv`: the Rye Beach chart, anonymized: bank, card, and loan account names lose institution names and trailing digits; account numbers, types, and every 4xxx/5xxx name are kept as they are. The owner supplies and approves it.
- `tests/fixtures/rye_beach/account_suggest_rules.json` and `account_map_expected.csv`: the slot scheme and the suggestions the owner expects, supplied with this brief. The JSON states the semantics (first matching rule wins); its shape may be changed to fit `account_suggest_rule`, the results may not. The expected file is the acceptance oracle; do not derive it from the code under test. These are expected *suggestions*: whether 5355, fuel, or anything else is finally job cost is the owner's call at confirmation and under D-04.

**Dependencies**: `openpyxl` for reading `.xlsx` (approved for this feature). Anything else: stop and ask.

### Out of scope
Any QuickBooks call or account sync (F05). Class-based or location-based division mapping (F20). Using the mapping to classify transactions (F10). Employee pay rates (F11). Deciding any accounting policy: F04 stores decisions, it does not make them. Cost categories beyond the D-23 list. Work-type codes (paving, planting, plowing): dropped by D-23; work-area names carry that detail. Whether Equipment (owned), Vehicles (owned), and Fuel reach jobs by direct coding or by allocation, and whether they are in the WIP basis (D-04, not decided). Estimates and work areas (F06/F07). Effective-dating `account_map` (approved periods are protected by snapshots in F16; changes are audited).

### Acceptance criteria
- [x] RLS enumeration passes for all seven new tables; the extra-policy allow-list still has one entry; tenant A reads zero rows of tenant B in each, via ORM and raw SQL as `app_rw`.
- [x] **Rye Beach fixture**: importing `chart_of_accounts.csv` (159 accounts) and running suggestions produces, for **every** account, exactly the division, cost category, and in-job-cost value in `account_map_expected.csv` (29 division cost accounts in job cost; income, headers, material sales, shared fuel, and everything outside 4000–5999 suggested as not job cost); every row has `status = suggested` and none is `confirmed` until a person confirms it.
- [x] Unmapped accounts are listed and counted; the count equals the number of active accounts without a confirmed mapping.
- [x] Importing the same chart file twice creates one batch and changes nothing. Importing a revised file (one account renamed, one added, one removed) renames, adds, and deactivates respectively; confirmed mappings on untouched accounts are unchanged; the renamed account keeps its mapping; raw history shows both versions.
- [x] With a synthetic `.xlsx` that mimics the owner's workbook layout (title row, section headings such as "ASSETS (1000–1999)", a legend block at the bottom), heading and legend rows are rejected individually; the batch ends `loaded_with_issues`; all real accounts load.
- [x] A suggestion never overwrites a confirmed mapping; re-running suggestions is idempotent.
- [x] A second tenant with no suggest rules imports a chart and gets zero suggestions and every account listed as unmapped.
- [x] `tenant_policy`: an accounting-policy key that was never set reads as not decided and `require_policy` raises naming the key; setting it requires `firm_admin`, records who, when, and a decision reference, and writes an audit row with before and after. No accounting-policy key has a default anywhere in code or migration (static test).
- [x] `burden_rate`: overlapping effective periods for the same division are refused; the rate in force on a given `date` is returned by one function, tested at the boundaries; rates are `Decimal` end to end (no `float` on the path; hygiene test extended).
- [x] `cost_category` holds exactly the D-23 list with its slots for a new tenant, seeded by the same audited service the API uses, per tenant, never by a cross-tenant statement. The cost-code function returns 410 for SNOW + Labor, nothing for a division without a `code_digit`, and the Cost codes view shows "no account" for Rye Beach cells such as SNOW + Subcontractors and every Equipment (owned), Vehicles (owned), and Fuel cell.
- [x] Role matrix extended: config read and write routes per the roles above; policy write is `firm_admin` only; 401 unauthenticated; CSRF header required.
- [x] Every state-changing action writes its audit row in the same transaction; "Confirm all suggestions" on N accounts writes N rows.
- [x] Frontend: no `style={{`, effect scanner passes with no new allow-list entry, `npm test` passes; money and rates shown through the shared formatting helpers; the Accounts table scrolls inside its container with the account column held in place and the page does not scroll sideways at 390 px.
- [x] `alembic upgrade head` → `downgrade base` → `upgrade head` on a scratch database; the migration is reversible; the downgrade guard still refuses `wip`.
- [x] CI green on the pushed commit.
- [ ] **Owner, in a browser**: upload the Rye Beach chart → see 159 suggestions, 29 of them in job cost, and the unconfirmed count at the top → open the Cost codes grid and see 410 against 5410 → correct one suggestion by hand → confirm all and see the unconfirmed count reach zero → set one policy key and see it recorded with your name → add a burden rate → check the Accounts page at phone width. Not ticked by Claude Code.
  - **Owner's pass, 2026-09-19, Chrome (recorded by Claude Code at the owner's instruction; the box stays unticked until the owner ticks it)**. First attempt failed at step 2: the batch stayed at Processing (fixed in "F04 · worker source-kind registration; batch never stranded at processing"). Second attempt, with the fixture csv, steps 1–10 all worked: 159 loaded, 0 skipped; 159 suggestions, 29 in job cost; Cost codes grid correct; one suggestion edited by hand; confirm all → 0 of 159 unmapped, 159 confirmed; `wip_basis` set with reference D-04; burden rate added, an overlapping one refused; Accounts at phone width scrolls inside its box with the account column held. Five items came out of the pass and were required before the tick (the owner's workbook layout, the zero-row guard, the zero-row message, the Source selection, the table pattern): see "F04 · owner workbook layout; zero-row guard; source selection kept; table pattern" in CHANGELOG.

### Plan (Claude Code fills in before coding)

**Fixtures (2026-09-18)**: `backend/tests/fixtures/rye_beach/` is the convention (BLUEPRINT §12 `tests/fixtures/`); the three files stay where commit 127dce9 put them. `account_map_expected.csv` was re-derived from `account_suggest_rules.json` by a throwaway script (not the code under test): 159 rows, zero mismatches, 29 in job cost (LS 9, EX 9, GC 6, SNOW 5). No row looks wrong. The oracle is never edited or regenerated.

**Acceptance criteria, restated**: seven tenant tables pass the RLS enumeration with the allow-list still at one; the Rye Beach chart (159 accounts) imported and suggested equals the oracle row for row, all `suggested`, none `confirmed`; unmapped = active accounts without a confirmed mapping, listed and counted on top; the same chart twice is one batch and no change, a revised chart renames/adds/deactivates with confirmed mappings and history intact; a synthetic workbook with title, section headings and legend rejects those rows individually and loads the rest as `loaded_with_issues`; suggestions never overwrite `confirmed` and are idempotent; a tenant without rules gets zero suggestions and everything unmapped; unset policy keys read "not decided", `require_policy` raises naming the key, setting is `firm_admin` with who/when/reference and a before/after audit row, no default anywhere (static test); burden rates refuse overlaps, one function returns the rate in force on a date (boundaries tested), Decimal end to end; `cost_category` is exactly the D-23 list per tenant seeded by the audited service, cost code 410 for SNOW+Labor, none without a `code_digit`, "no account" cells on the grid; role matrix extended; N audit rows for confirm-all on N accounts; frontend static checks, `.table-wrap` with the account column held, no sideways scroll at 390 px; migration round-trips on a scratch DB with the guard still refusing `wip`; CI green; owner browser pass (not ticked by Claude Code).

**Files to create**
- `backend/app/domain/__init__.py`, `backend/app/domain/config/__init__.py`, `models.py` (`Division`, `CostCategory`, `GlAccount`, `AccountMap`, `AccountSuggestRule`, `TenantPolicy`, `BurdenRate`), `categories.py` (D-23 list + `seed_cost_categories(db, tenant_id, actor)` + `cost_code(division, category)`), `chart.py` (normalizer: raw → `gl_account`; `suggest(db, tenant_id)`), `rules.py` (rule matching; fixture JSON → rows loader), `policy.py` (`POLICY_KEYS`, `get_policy`, `set_policy`, `require_policy`, `PolicyNotDecided`), `burden.py` (`add_burden_rate`, `burden_rate_on(db, tenant_id, day, division_id)`), `service.py` (audited create/change/confirm/deactivate for each table; `confirm_all_suggestions`).
- `backend/app/integrations/chart_of_accounts.py`: source kind `chart_of_accounts` (`.csv`, `.xlsx`; `RawItem` per four-digit account, `RejectedItem` otherwise; XLSX cells read as text, never via `float`).
- `backend/app/api/config.py`: `/api/config/{accounts,account-map,divisions,cost-categories,cost-codes,burden-rates,policy,suggest-rules}`; `can_view_tenant_config`, `can_manage_tenant_config`, `can_set_policy` in `authz.py`.
- `backend/alembic/versions/0005_tenant_configuration.py` (seven tables; reversible).
- Tests: `test_config_models.py` (RLS rows per table), `test_chart_import.py` (fixture, oracle, revised chart, workbook), `test_suggest.py`, `test_policy.py` (incl. the no-default static test), `test_burden.py`, `test_cost_codes.py`, `test_config_api.py`; `test_roles.py`, `test_hygiene.py` (`float(` scan extended to `app/domain/`), `test_rls.py` extended.
- Frontend: `pages/Accounts.jsx`, `Divisions.jsx`, `CostCategories.jsx`, `CostCodes.jsx`, `BurdenRates.jsx`, `Policy.jsx`; `src/rates.js` (`formatRate`, string in, four decimals as a percent, tests with `node --test`); nav in `Shell.jsx`.
- Docs: OPERATIONS (loading a chart, editing suggest rules, setting a policy key), CLAUDE.md Vocabulary (D-23 list verbatim + the "Cost code" line, as D-23 says), `docs/briefs/F04.md`.

**Dependency**: `openpyxl` (approved). Nothing else.

**Open questions — proposed answers (awaiting go-ahead)**
1. **Name of the chart table.** BLUEPRINT §5 names no chart-of-accounts table at all (it is the job spine). §7's configuration line is `account_map(gl_account → division, cost_category, in_job_cost bool)`: `gl_account` appears only as the thing an `account_map` row points at, and §6.2 lists `Account` among the QBO entities. Nothing conflicts, so keep `gl_account` as specified here; F05 fills `external_id` from QBO `Account.Id`, matched by `account_no` (`AcctNum`).
2. **Shape of `account_suggest_rule`.** One row per rule, `sort_order` unique per tenant, first match wins, and a rule sets each of the three outputs in one of three ways: `pattern` (text, a regular expression anchored to the whole four-digit account number); `division_id` **or** `division_from_digit` (1–4: the position in the account number whose digit is looked up in `division.code_digit`; no division with that digit → the rule does not match and the next one is tried); `cost_category_id` **or** `cost_category_from_slot` (boolean: the last two digits are the slot; no category with that slot → no match); `in_job_cost` NOT NULL; `active`; `name`. Rye Beach as rows: seven income rules (`^4100$` → LS … `^4800$` → DES, not job cost); `^5[1-4]00$` division from digit 2, no category, not job cost; `^5[1-4]\d\d$` division from digit 2, category from slot, **job cost**; `^5500$` MS, no category; `^55\d\d$` MS + Materials, not job cost; `^5600$` no division, no category; `^56\d\d$` Fuel, not job cost; `^.*$` nothing, not job cost (14 rules). The fixture JSON keeps its results and gets this shape (a loader turns it into rows; the semantics note stays). Rules are data: the Accounts page shows the matched rule name, editing rules is an OPERATIONS runbook in F04 (JSON re-load), a screen later if wanted.
3. **Triggering the normalizer.** A chained task. `SourceKind` gains `after_load: str | None` — the task kind to enqueue when a batch ends `loaded` or `loaded_with_issues`; `process_batch_now` enqueues it inside the batch's final status transaction (same tenant, same `tenant_session`, so one tenant per transaction and the enqueue commits only with the status) with `dedupe_key = f"{after_load}:{batch_id}"`. `chart_of_accounts.after_load = "config.normalize_chart"`, whose function runs the normalizer then `suggest()` under its own `tenant_session`s. The import pipeline stays free of source branches (CLAUDE.md Integrations); a second registered step would need the worker to know about steps. Re-running the chained task is safe: the normalizer is idempotent and suggestions never touch `confirmed` rows.

**Owner answers (2026-09-18)**: Q1 keep `gl_account`; Q2 and Q3 approved as proposed, with three amendments, and the domain code lives under **`app/domain/config/`** (CLAUDE.md: domain logic lives in `app/domain/`; `app/config/` had no advantage).
- **A. Catch-all scope.** The final rule matches only account numbers outside 4000–5999 (`^(?![45]\d{3}$).*$`). An income or cost-of-goods-sold account that matches no earlier rule gets **no** suggestion and is listed as unmapped. Rule 6 of `account_suggest_rules.json` updated on the owner's instruction (semantics note too); `account_map_expected.csv` unchanged and still matches row for row. Test: adding account 5195 "Misc - LS" (unknown slot) yields no `account_map` row and it appears in the unmapped list and count.
- **B. No four-digit assumption in the engine.** `account_no` is text of any length; patterns are anchored to the whole string (`re.fullmatch`); `division_from_digit` is a 1-based position in that string, not limited to 1–4. The `chart_of_accounts` source rejects a row when the account-number cell is empty or is not an account number (headings, legend, title), not when it is not exactly four digits. Patterns are compiled and validated when rules are loaded (invalid, or over 100 characters → refused naming the rule). Test: a second tenant with five-digit numbers and its own rules gets correct suggestions.
- **C. Follow-on visibility.** The batch API returns the state of its `after_load` task (`followup_status`, `followup_label`, and the one-sentence `message`: "Loaded. Updating accounts…" / "Loaded. Accounts updated." / "Loaded, but the accounts could not be updated. Try uploading again, or contact support."); the Imports list shows it. The machine detail goes to the task's `last_error` and the log (surfaced only in `error_detail`), never the screen. `import_batch.followup_task_id` links the batch to the task. Tests cover all three states.

### Discovered (do not fix here)
_Things noticed along the way that belong to another feature._
- **Suggestion rules have no screen** (F04 built them as data loaded by script or `PUT /api/config/suggest-rules`; the Accounts page shows the matched rule name). A screen to edit rules can come with F20 (the second tenant, Classes) when a second scheme exists.
- ~~**Cost categories are seeded lazily**~~ Done 2026-09-18 (F04 · seeding at tenant creation): `create_tenant_and_seed` seeds the D-23 list in a second transaction under the new tenant's context, API and CLI; the lazy path stays as a safety net for tenants that predate F04, attributed to system, one set under concurrency.
- **`gl_account.ledger_type` is text as the source gives it** ("Cost of Goods Sold", "Bank"…); F05 will bring QBO's `AccountType`/`AccountSubType`. Nothing maps the two yet.
- **Policy values in API responses**: money is returned as a string (`small_job_threshold`), the F08 rule applied early; the response scan does not yet assert "no money as a JSON number" generally, only per test.
- **Why the suite missed the worker's missing source kind (owner browser pass, 2026-09-19; fixed in "F04 · worker source-kind registration; batch never stranded at processing")**: the tests run the API and the `Worker` in one process, and `conftest` imports `app.main`, whose import chain registered `chart_of_accounts`; `python -m app.worker` never imports `app.main`, so the real worker knew only `unparsed_file`. The one fresh-process test started the API, not the worker. Rule from now on: anything registered by import (task kinds, source kinds) has a fresh-process test per process that needs it, and source kinds load from `SOURCE_KIND_MODULES` only.
- ~~**The real Rye Beach workbook does not parse**~~ Fixed 2026-09-19 (2792364): the workbook keeps column A empty (No. / Account Name / QuickBooks Type in B–D, header in row 4, three sheets) and `parse_chart` now finds the account-number column. **Evidence that the layout is supported: the stored workbook, parsed locally from the object store, gives 160 accounts loaded and 9 rows skipped.** By the owner's decision (2026-09-20) the workbook is not re-processed on the dev database, now or later: the fixture csv's names are the deliberate anonymization from this brief and the dev rye-beach tenant stays on the fixture. The 2026-09-19 06:13 batch row stays as it ended (Loaded with issues, 0 loaded, 170 skipped).
- ~~**Re-running an older chart batch overwrites names from a newer one**~~ Fixed 2026-09-20 (F04 · nothing loaded status; only the newest chart is applied): `normalize_chart` applies a batch only if it is the tenant's newest chart batch by `uploaded_at`; an older batch processed late changes nothing and its follow-up reads "Not applied".
- ~~**The runbook's requeue covers `failed` tasks only**~~ Owner, 2026-09-20: no requeue path for succeeded tasks is needed; a corrected file is a new batch.
- **Imports defaults to "Unparsed file" on a first visit (2026-09-19)**: the choice is now kept per company for the session, but a first upload can still go under the wrong source. An empty first option ("Choose a source") would make the choice explicit.
- ~~**Status for a batch from which nothing could be read**~~ Decided by the owner 2026-09-20 and built: `nothing_loaded`, shown as "Nothing loaded", an end state that is not `failed` (migration 0006).
- **For the owner: the real chart has one account the fixture does not, 2630.** The fixture (159 accounts) and the oracle are the owner's files and were not touched; the owner will supply an updated fixture and oracle if 2630 should be added.

### Close-out
- [x] CHANGELOG entry written
- [x] ROADMAP status flipped (◐ until the owner's browser pass; then ☑)
- [x] OPERATIONS.md: loading a chart, editing suggest rules, setting a policy key
- [x] D-23 present in `docs/DECISIONS.md`
- [x] Brief copied to `docs/briefs/F04.md`; live file rewritten as a stub pointing at F05
- [x] One commit, not pushed
